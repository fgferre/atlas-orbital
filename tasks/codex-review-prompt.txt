You are doing an INDEPENDENT CRITICAL REVIEW of a planning document.
Do NOT write any code. Do NOT edit any files. Your output is a review
report only.

## Context

This repo (atlas-orbital) is a Three.js + React Three Fiber orbital
visualizer. We just finished planning "Phase θ — Gaia Sky-inspired
visual upgrade". The plan is a port of high-impact visual techniques
from the Gaia Sky project (https://github.com/langurmonkey/gaiasky,
Java/LibGDX) to our Three.js/R3F/TypeScript stack.

Two prior reviews already ran:
1. A Sonnet-class agent explored the Gaia Sky repo and proposed the
   original 8 ondas (θ.1–θ.8).
2. A 15-agent Haiku swarm validated the Sonnet report and found 6
   additional ondas (θ.9–θ.14) plus a correction section §8.6.

## Your job

Third independent pass. Cross-check that we have NOT left any of our
own hard-won learnings behind, AND have not missed any high-impact
Gaia Sky visual technique that a third reviewer would catch.

## Files to read (in this order)

1. tasks/phase-gaia-sky.md — the plan under review.
2. tasks/lessons.md — our accumulated lessons L1–L21. Verify the plan
   respects EVERY lesson that is relevant to shader, composer, camera,
   or store work. Call out any onda whose parameters or implementation
   approach risks violating an L-lesson.
3. tasks/todo.md §"Phase θ" pointer block (around line 11) — verify
   consistency with phase-gaia-sky.md.
4. AGENTS.md — verify the plan respects the project's mandatory
   principles (especially #3 smallest change, #5 testable functions,
   #14 full lifecycle including edge/error/loading/empty, #15
   performance).

## What to look for (ranked by priority)

A. **Lessons we might have forgotten.** Any L-lesson in lessons.md
   that the phase does NOT explicitly reference or protect against.
   Be ruthless — if a lesson is about "never do X on the hot path"
   and an onda touches the hot path, the onda spec must say how it
   avoids X.

B. **Gaia Sky techniques still missing.** Use your training knowledge
   or, if you have web access in your toolset, the Gaia Sky repo
   directly. Flag any high-visual-impact technique from Gaia Sky that
   is NOT in our 14 ondas and NOT in §9 out-of-scope. Examples of
   what to check for (not exhaustive, use your judgment):
   - Gaia Sky's star shader variants (v3.7.0+ has 3 billboard
     shading modes per §8.6)
   - Depth-of-field / focal-plane blur
   - Shadow casting between bodies (eclipse shadows)
   - Anti-aliasing strategy (FXAA vs SMAA vs TAA; Gaia Sky uses
     several)
   - Ambient occlusion for terrain in surface mode
   - Asset tiling / texture streaming at close approach
   - Halo subtraction or negative bloom for planets against stars
   - Specific post-processing passes we have not named
   - Per-quality-tier shader LOD (simpler fragment shaders on
     constrained tier beyond just disabling effects)
   - Color management pipeline (Rec.709 vs sRGB, gamma handling
     at intermediate composer stages)
   - HiDPI / supersampling strategy

C. **Sequencing errors.** Review §8's 14-onda ship order. Flag any
   case where onda N's verification would be UNRELIABLE because a
   later onda would change the baseline. Our updated ordering is:
   θ.1 → θ.13 → θ.6 → θ.2 → θ.9 → θ.10 → θ.12 → θ.8 → θ.3 → θ.4 →
   θ.5 → θ.14 → θ.11 → θ.7a → θ.7b.

D. **Internal inconsistency.** Any contradiction between:
   - phase-gaia-sky.md §2 (non-goals) and any onda's spec.
   - phase-gaia-sky.md §10 (exit criteria) and §8 (sequencing).
   - todo.md Phase θ block and phase-gaia-sky.md ondas.
   - Performance claims and the tier strategy in §4.

E. **Risks we are NOT acknowledging.** Scan §6 risk register. What
   high-probability risks would a staff engineer add that we didn't?

## Output format

```
# Phase θ — Codex independent review (2026-04-20)

## Summary verdict

One sentence: SHIP-READY / SHIP-READY-WITH-AMENDMENTS / BLOCKED.

## High-severity findings

Numbered list. For each:
- **Finding:** one-sentence claim.
- **Evidence:** file path + line numbers or quoted text.
- **Impact:** what breaks if we ship without fixing this.
- **Recommended fix:** concrete, actionable, under 3 sentences.

## Medium-severity findings

Same format. Cap at 10.

## Low-severity / nice-to-have

Bullet list, no structure required. Cap at 10.

## Lessons audit

For each of L1–L21 in lessons.md, one line:
`L##: RESPECTED | AT-RISK in θ.X (reason) | N/A for this phase`.

## Missing Gaia Sky features (your pick)

Up to 5, ranked by visual impact. For each: one-line description +
whether it should be folded into an existing onda, added as a new
onda, or deferred to §9.

## Do-not-fix (deliberate scope control)

Anything you considered flagging but chose not to, with one-line
rationale each. This protects the user from over-scoping.
```

## Hard constraints on YOUR review

- Do NOT write any code.
- Do NOT edit any files in the repo.
- Do NOT propose a completely new plan — critique the existing one.
- Quote specific file:line refs for all evidence.
- Be terse. No preamble, no restatement of the plan.
- Assume the reader has already read phase-gaia-sky.md.
