# T6.4 — Visual recovery wave (M1-M5+M7 core ~8-13 h; M6 forward-port ~14 h, parallelizable post-M2.5)

**Status**: PRIORITY 0 (blocking T-Closeout, T4.3, T5.x re-bakes).
**Surfaced**: 2026-05-04 first user-driven manual smoke.
**Plan revisions**: 4 Codex audit passes (round-1, round-2,
round-3, final-pass) consolidated into this canonical wave file
per L38 (single-source-of-truth restructure).

---

## Why this wave exists

User-driven manual smoke (2026-05-04) revealed T6.0→T6.3-ε wired
the FULL pipeline correctly (focus path, fly-to, mesh mount,
skipMask suppression, click handler, all gate math) but the
procedural mesh **never renders visually** at HYG positions.
Root-caused via live debugging + `gpt-5.2` Codex round-3 audit.
Four root causes, all silent (no console errors / test failures /
lint warnings — entire infrastructure correct, visual output empty).

Full narrative + 4-root-cause breakdown in
`tasks/archive/postmortems/T6-visual-failure.md`.

---

## Scope tag

**Hybrid**: Gaia-informed (M1+M2 follow Gaia's
`ParticleSet.fetchPosition:1112-1114` quad-precision camera-relative
subtract pattern, adapted to Three.js's `Matrix4.multiplyMatrices`
which is float64 CPU-side) + atlas-native (M3-M7 — smooth fade,
class variation extension, spect fallback, info panel).

Per-milestone scope tags below. DIFF GATE applies line-by-line
to Gaia-informed milestones; per-decision rationale to atlas-native.

---

## Wave-specific protocol delta (2026-05-05)

The canonical Loop protocol lives in `tasks/STATUS.md` §Loop
protocol (15 steps). T6.4 ADDS one delta on top of that:

**Codex audit per milestone before commit** (between STATUS
step 12 "Codex audit (per user request)" and step 13 "Commit"):
each milestone runs Codex audit on the uncommitted diff, asking
for blindspots/bugs/divergences/scope gaps with an unbiased
prompt bounded to the milestone diff scope. Findings get
verified against source first per
`feedback_codex_findings_toward_1to1.md` (Codex can be wrong);
real findings addressed before commit. No bypass.

The 7 Codex-caught surface bugs across T6.3-δ + T6.3-ε proved
external cold-read reliably catches what internal SUBAGENT
VERIFY misses on atlas-native code; the post-restructure audit
caught further structural drift in the docs themselves.

**Do NOT define a parallel milestone protocol here.** STATUS is
canonical; this section only lists what T6.4 adds.

---

## M1 — Sphere shader → modelViewMatrix (precision fix) ✅ PASS

**Effort**: ~30min-1h (actual: ~1h incl. dev-exposure smoke +
Codex audit).
**Scope tag**: Gaia-informed (Three.js `modelViewMatrix` is
the `ParticleSet.fetchPosition` analog).
**Status**: DONE 2026-05-05 (commit `74d6cea`). Verified via
preview-MCP smoke at HYG focus `hyg:0` (Sirius, world position
~5.3×10^8 atlas units). Mesh visible after fly-to + zoom; gate
fired (`meshActive: true`, `sa: 0.040` at distToCamera=194).
Sun-at-origin still renders identically.

Minimal change to validate the precision hypothesis. Replaced
`gl_Position = projectionMatrix * viewMatrix * (modelMatrix * vec4(position, 1.0))`
with `gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0)`.

Three.js computes `modelViewMatrix` CPU-side as
`cameraMatrixWorldInverse × objectMatrixWorld` via
`Matrix4.multiplyMatrices` (plain JS, float64). Translation column
of result = `(objPos - cameraPos)` computed in float64 (precise
small value), cast to float32 only for GPU upload — same trick
Gaia uses, leveraging Three.js's existing infrastructure instead
of porting Vector3Q quad math.

The `vWorld` varying for fragment side became `vViewPos`
(camera-relative). Fragment's
`vec3 Vview = normalize((viewMatrix * vec4(vWorld - cameraPosition, 0.0)).xyz)`
became `vec3 Vview = normalize(vViewPos)`.

**Files touched**:

- `src/components/canvas/shaders/proceduralSunShaders.ts` —
  sphere vertex (lines 156-210) + fragment (lines 212-284). Glow
  / rays / flares unchanged (M2 territory).
- `.gitignore` — added `.codex/config.toml` (Codex audit P2 sweep).

**Codex audit (gpt-5.5, `codex exec review --uncommitted`)**:
clean for shader; 1 P2 — untracked `.codex/config.toml` not
gitignored (would leak `danger-full-access` if committed).
Addressed in same commit via `.gitignore` line.

**Acceptance** (met): clicking Sirius → camera flies → procedural
sphere mesh appears (visible, not invisible). Validated via
preview-MCP screenshot at `hyg:0` focus + zoom; orange disc
clearly rendered with skipMask suppressing the sprite slot 0.
M1 PASS, proceed M2.

---

## M2 — Glow + rays + flares shaders camera-relative ✅ PASS

**Effort**: ~2-3h (actual: ~2h incl. Codex P1 GLSL ES 1.0
compatibility fix).
**Scope tag**: Gaia-informed (same modelViewMatrix pattern).
**Status**: DONE 2026-05-05 (commit `ca65f71`). Verified via
preview-MCP smoke at both `sun` and `hyg:0` (Sirius) focus
followed by zoom-in: full procedural sun renders with sphere,
glow halo, rays, and flares all visible at parsec-scale HYG
positions; Sun-at-origin renders identically post-M2 (no
regression). M1+M2 together close the visual-recovery
precision-collapse failure mode.
**Depends on**: M1 PASS ✓ (commit `74d6cea`).

Applied modelViewMatrix pattern to all 3 billboard shaders.
Glow's `vec3 toCamera = normalize(center - cameraPosition)`
became view-space `vec3 toCenterView = normalize(centerView)`
where `centerView = (modelViewMatrix * vec4(0,0,0,1)).xyz`.
Camera-up `uCamUp` (world-space) became canonical view-space
`(0,1,0)` and the uniform was removed from JS (`cameraUpRef`
plus per-frame setter also dropped). Rays' world-space
`p0w = (modelMatrix * vec4(p, 1.0)).xyz` plus
`vW = normalize(p0w - cameraPosition)` became view-space
`p0v = (modelViewMatrix * vec4(p, 1.0)).xyz` plus
`vView = normalize(p0v)`. Flares similar.

### Coordinate-space contract (per Codex round-3 P2, applied)

Shipped option (2): geometry in view space, lighting dot-product
in world space using `vNormalWorld` (glow) / `vNormal` (rays,
flares). Each billboard vertex computes its outward radial
direction in view space, then converts to world via
`outwardView * mat3(viewMatrix)` — mathematically identical to
`transpose(mat3(viewMatrix)) * outwardView` but **GLSL ES 1.00
compatible**: `transpose()` is a GLSL ES 3.00+ builtin, and
atlas's ShaderMaterial path is on the WebGL1 baseline. Codex
audit P1 caught this; the fix was a syntactic-only swap. Contract
pinned in shader comments at every site (glow-vertex header,
glow-fragment header, rays-vertex inline, flares-vertex inline).

### Glow normal correctness sub-fix

The pre-M2 glow fragment did `getAlpha(normalize(vWorld))`,
treating world-space position as a normal. For Sun-at-origin
this approximated the outward radial direction (since the Sun
is at world origin); for HYG stars at parsec scale, `vWorld`
was dominated by the star's position offset and the dot-product
became meaningless. M2 introduces `vNormalWorld` as a true
per-fragment outward radial varying. Visible only as a
correctness improvement once HYG meshes render (post-M1).

**Files touched**:

- `src/components/canvas/shaders/proceduralSunShaders.ts` —
  glow vertex+fragment (lines ~286-379), rays vertex (lines
  ~441-484), flares vertex (lines ~547-629). Sphere shaders
  unchanged (M1 territory).
- `src/components/canvas/ProceduralSun3D.tsx` — removed three
  things: the `uCamUp` uniform from the glow material init,
  the `cameraUpRef` ref declaration, and the per-frame
  `glowMaterial.uniforms.uCamUp.value.copy(...)` setter.
- `.gitignore` — added `.claude/scheduled_tasks.lock` (Codex
  audit P3 sweep).

**Codex audit (gpt-5.5, `codex exec review --uncommitted`)**:
P1 GLSL ES 1.00 incompatibility caught (`transpose` builtin
missing) — Codex even ran a Playwright shader-compile test to
verify. Resolved in same commit by switching to
`outwardView * mat3(viewMatrix)`. P3 untracked lock file —
gitignored in same commit.

**Acceptance** (met, with one user-driven item outstanding):

- ✅ Full procedural sun visible at HYG positions (sphere +
  glow halo + rays + flares all render at `hyg:0`).
- ✅ Sun at origin renders identically post-M2 (verified by
  zoomed-in `sun` focus screenshot — sphere, corona, rays, and
  flares all present and matching the pre-M2 baseline).
- ✅ Coordinate-space contract pinned in shader comments at
  every site.
- 🟡 User-driven smoke (4 named stars × 2 zoom cycles per
  ROADMAP §T6.3) — agent-driven smoke covered Sirius and Sun
  only; user should drive the full 4-star × 2-cycle pass before
  declaring T6.4 core shipped.

---

## M2.5 — HYG flight contract (Gaia-informed, Atlas-safe)

**Effort**: ~6-8h (was ~5-7h pre-2026-05-05 Codex correction;
expanded after the angle-semantics fix + Atlas mesh-spawn clamp

- explicit OrbitControls coordination strategy).
  **Scope tag**: Gaia-informed for the math + transition contract;
  atlas-native for the mesh-spawn clamp, the `OrbitControls`
  coordination, and the interrupt path.
  **Status**: S1-S7 + Codex round-3 hotfix shipped; agent-complete
  pending the user-driven smoke. Shipped: S1 (`c44cebe` + `e580288`
  Codex hotfix), S2 (`e6d35de`), S3 (`ccd7d2f`), S1+S3 hotfix per
  Codex 2026-05-05 round-2 review (cancel `onComplete` leak, 10 wu
  absolute landing floor restored, `+Infinity` edge-case fix,
  `f54425d`), S4 (`CameraController` HYG branch rewrite — drops
  the orientation snap + Bézier sun-avoid path, consumes
  `StellarFlightTransition` with scale-aware durations, wires
  real per-star physical radius to `near`/`minDistance`,
  `cf24711`), S5 (`OrbitControls "start"` handler calls
  `stellarFlightRef.cancel()` and syncs `controls.target` to the
  frozen intermediate state — no orientation snap-back, no
  position jump on user mouse-drag mid-fly, `8cd6f2e`), S6 (mesh
  pre-warm channel — `hygFlightPosProgress` singleton + new
  `posProgressRaw` getter on `StellarFlightTransition`, `3fc9939`.
  S6 originally force-activated the procedural mesh once raw
  alpha cleared 0.70, but Codex round-3 C-2 reverted that
  force-activate because writing `skipMask=1` mid-fly hid the
  sprite while the mesh was still angularly small — exactly the
  gap M2.5 was meant to avoid. The singleton + getter stay in
  place so M3's cross-fade can consume the channel as a
  continuous fade ramp instead of a hard force-on), S7 (test-store hook
  - Playwright `e2e/hyg-focus.spec.ts` regression — Sirius focus
    survives the fly-to without errors or defocus, `6d63f1d`),
    **Codex round-3 hotfix** (4-finding bundle: C-1 angular-radius
    math fix dropped the 2× landing-distance regression; C-2
    reverted the S6 force-activate that hid the sprite without M3
    cross-fade; C-3 honest comments downgraded "Gaia-faithful" to
    "Gaia-informed" where the contract diverges; C-4 strengthened
    e2e via `__ATLAS_TEST_CAMERA__` + `__ATLAS_TEST_MESH_STATE__`
    hooks asserting target-lerp, landing-distance bracket, mesh
    active state, skipMask timing).
    **Remaining**: user-driven smoke (4 named stars × 2 zoom
    cycles) before M2.5 ships per the wave's Acceptance §8.
    **Depends on**: M2 PASS ✓ (commits `ca65f71` + `bd79311`).
    **Blocks**: M3 (smooth sprite↔mesh fade is meaningless if the
    camera is still mid-snap on arrival).

### Scope discipline (per Codex 2026-05-05 review)

This is **NOT** a full AAA navigation rewrite. The scope is
specifically: replace the snap+Bézier+single-channel HYG fly-to
with a Gaia-informed two-channel transition that respects the
Atlas mesh-spawn gate. "Gaia-informed" rather than "Gaia-faithful"
because the orientation channel is an OrbitControls-native
approximation of Gaia's quaternion slerp, not a port (see Codex
round-3 §C-3 below). Curated-body fly-to stays untouched.
The interactive/scripted "go-to-object continuous push" mode
from Gaia's `InteractiveCameraModule` is out of scope — Atlas
uses pre-computed transitions, not REPL-style camera scripting.

### Why this milestone exists (2026-05-05)

User smoke after M1+M2 surfaced that the HYG fly-to feels
janky despite the mesh now rendering correctly. Source trace
of the existing path (`CameraController.tsx:230-317` plus
`CameraTransition.ts`) plus reading the actual Gaia Sky source
identified six root causes:

1. **Orientation snap pre-animation**: `controls.target.copy(targetPos)`
   at `CameraController.tsx:315` runs BEFORE
   `transitionRef.start()`. The camera "rotates instantly" to
   look at the star while position is still translating —
   classic spin-then-glide feel.
2. **Single-channel transition**: `CameraTransition` interpolates
   only position. Three.js `OrbitControls` derives orientation
   from `target`; with target snapped, orientation has no
   independent timing.
3. **Sun-avoidance Bézier control point**:
   `CameraTransition.ts:56-86` pushes the curve away from the
   Sun. Designed for solar-system geometry; at parsec scale
   the Sun is one star among many and the arc looks weird.
4. **Asymmetric easing** `easeOutQuint`: starts fast, ends
   slow. Cinematic feel needs a symmetric S-curve (sigmoid)
   so both ends feel deliberate.
5. **Fixed target solid angle**: Atlas computes
   `idealDist = radius / (STELLAR_MESH_ENTER_RAD * 5)` ≈
   0.286° apparent size. Far stars and near stars get the
   same target → near stars land smaller than they should,
   far stars larger (or below the gate).
6. **Hard duration cap (4s) regardless of distance**: an
   interstellar fly-to from solar system to a 100-pc star
   gets the same time budget as a fly-to within the solar
   system. Position scale-aware duration is missing.

### Gaia Sky reference (verified 2026-05-05)

Source citations (clone at `/tmp/gaiasky`):

- **Two-channel transition** with separate quaternion-slerp
  orientation and path-interpolated position:
  [`CameraModule.java:1404-1425`](file:///tmp/gaiasky/core/src/gaiasky/script/v2/impl/CameraModule.java).
  `posInterpolator.valueAt(positionMapper(alphaPos))` for
  position; `startOri.slerp(endOri, orientationMapper(alphaOri))`
  for orientation. Two `alpha` variables clamped to two
  separate durations.
- **Distance from solid angle**:
  [`CameraModule.java:665`](file:///tmp/gaiasky/core/src/gaiasky/script/v2/impl/CameraModule.java)
  `targetDistance = radius / FastMath.tan(targetAngle * 0.5)`.
- **Default solid angle by object type**:
  [`CameraModule.java:660-664`](file:///tmp/gaiasky/core/src/gaiasky/script/v2/impl/CameraModule.java)
  8° for ParticleSet (catalog stars), 20° for curated bodies.
- **Adaptive solid angle for catalog stars** (the nuance the
  first-draft analysis missed):
  [`InteractiveCameraModule.java:158-168`](file:///tmp/gaiasky/core/src/gaiasky/script/v2/impl/InteractiveCameraModule.java)
  linear lerp `1.0° at 1.31 pc → 0.001° at 2805 pc` based on
  the star's distance from the Sun. Per Codex round-3 the lerp
  values are ANGULAR RADIUS (not full angle): the Gaia consumer
  at `InteractiveCameraModule.java:172` compares against
  `focusView.getSolidAngle()` which `ParticleSet.java:809-810`
  defines as `(radius/distance)/fovFactor` — angular-radius-like
  semantics. Reference values along the curve (ANGULAR RADIUS):
  Sirius (2.6 pc) → ~0.9995°; 100 pc → ~0.965°; 1000 pc →
  ~0.644°; **2003 pc → ~0.286° (this is the crossover where
  Gaia's adaptive target equals the Atlas mesh-spawn floor of
  5e-3 rad ≈ 0.286°)**; 2805 pc → 0.001°. Pre-Codex-round-3 the
  crossover was at ~1200 pc because the lib halved the curve
  before comparing — see C-1 in §Codex-round-3. Earlier draft
  incorrectly stated "M dwarf at 100 pc lands at ~0.05°" —
  that was off by ~20×; corrected by Codex 2026-05-05 round-2.
- **Default easing `logisticsigmoid`** with smooth factor 60
  ([`CameraModule.java:676`](file:///tmp/gaiasky/core/src/gaiasky/script/v2/impl/CameraModule.java)):
  symmetric S-curve, both starts and ends softly.

### Spec

#### S1 — Angle math + Atlas mesh-spawn clamp (~1.5h, shipped at `c44cebe` + Codex round-1 correction + Codex round-3 angular-radius fix)

`src/lib/camera/stellarFlightSolidAngle.ts`. Two-layer contract:

1. **Gaia-informed**: `computeGaiaTargetAngularRadiusRad(distancePc)`
   returns the ANGULAR RADIUS in radians from the lerp
   `1.0° at 1.31 pc → 0.001° at 2805 pc`. Source:
   `InteractiveCameraModule.java:158-172` compared against
   `ParticleSet.java:809` (`focusSolidAngle =
(radius/distance)/fovFactor` — angular-radius semantics
   despite the misleading name). "Gaia-informed" rather than
   "Gaia-faithful" because Gaia normalises by
   `camera.getFovFactor()` and computes radius from a
   pseudo-size (`size × STAR_SIZE_FACTOR`,
   `ParticleSet.java:785`); Atlas drops the fovFactor
   normalisation and uses physical radius from `radiusFromSpect`.
   Both divergences are pinned in the lib header.
2. **Atlas-clamped**: `computeAtlasFlightTarget(distancePc)`
   wraps Gaia's target with a floor at the procedural-mesh
   spawn gate. Without the clamp, a star at the far anchor
   (Gaia 0.001° angular radius = 1.745e-5 rad) lands far
   below `STELLAR_MESH_ENTER_RAD = 1e-3` and the procedural
   mesh NEVER spawns — the camera arrives correctly per Gaia
   but T6 visual recovery silently regresses for distant
   stars. The clamp ensures
   `angularRadiusRad ≥ STELLAR_MESH_ENTER_RAD × 5`, which
   matches the 5× hysteresis margin the pre-M2.5
   `CameraController.tsx` already used.

Convenience composer
`computeAtlasFlightLanding(radiusWu, distancePc)` returns
`{ distanceWu, target: { angularRadiusRad, clampedByAtlasFloor }
}` so the camera controller (S4) can log which clamp
dominated.

**Angle semantics — pinned in lib header to prevent
re-confusion** (Codex round-3, 2026-05-05, after the M2.5
first-draft confused angular radius with full angle):

- **Angular radius** (apparent radius) — the unit Gaia's
  `getSolidAngle()` actually returns despite its name, and the
  unit Atlas's mesh-spawn gate compares against. The lerp
  anchors (1.0°, 0.001°) live in this space.
- **Full angle** (apparent diameter, = 2 × angular radius) —
  NOT used in this lib post-Codex-round-3. The first-draft
  M2.5 lib treated the Gaia anchors as full angle and halved
  them, producing a 2× landing-distance regression for every
  HYG fly-to.
- **Solid angle** (sr) — area on the unit sphere. NOT used
  here; the unfortunate `solidAngle` name in Gaia's source is
  preserved only in historical-reference comments.

Crossover where Gaia's adaptive angular radius equals the
Atlas floor sits at ~2003 pc (post-Codex-round-3). Below that
distance the camera lands per Gaia (closer than pre-M2.5 —
Sirius lands at ~456 wu vs pre-M2.5 ~1593 wu, a ~3.5× ratio).
Beyond, it lands at the pre-M2.5 floor exactly (no regression).

Tests pin: anchor constants byte-equal to Gaia source, lerp
behavior at exact anchors + midpoint + clamp regions,
named-star landings (Sirius, Betelgeuse, Proxima, Sun-at-
far-anchor), crossover behavior near 2003 pc, mesh-spawn
floor never violated across the full distance range. The
"regression vs Atlas pre-M2.5" pin uses the **correct**
pre-M2.5 angular radius (5e-3 rad), giving the ~3.5× ratio
above. The Codex-round-1 corrected commit pinned ~1.74×
because the lib was still halving the Gaia curve — that
mid-state was wrong; round-3 dropped the halving and
restored the original ~3.5× ratio.

#### S2 — Logistic-sigmoid easing (~30min, shipped at `e6d35de`; factor bumped to Gaia default in round-5)

Added `CameraTransition.logisticSigmoid(t, factor=60)` static
method (`src/lib/camera/CameraTransition.ts:185-220`). 13 unit
tests pin endpoint clamps (`logisticSigmoid(0) === 0`,
`logisticSigmoid(1) === 1`), midpoint symmetry, point-symmetry
around (0.5, 0.5), monotonicity, [0, 1] bounds, S-curve
character at t∈{0.05, 0.4, 0.6, 0.95}, factor parameter
behavior, and the raw-sigmoid formula match. Endpoints clamped
via affine renormalization
`(sig(t) - sig(0)) / (sig(1) - sig(0))` so the start/end frames
don't visually freeze (raw sigmoid at factor=60 sits at ~9e-14
from 0/1 without the renorm).

**factor=60 is Gaia's default** (`CameraModule.java:1210` clamps
the smoothing factor to `[12, 500]` and Gaia's Settings feeds
60). Pre-round-5 atlas defaulted to 12 as a documented
"intentional Atlas-Gaia divergence" — the 12 choice was made
without empirical user data, on the assumption Gaia's 60
"feels stalled" for 4-6s transitions. **User smoke
2026-05-05 disconfirmed** that judgment: factor=12 made every
solar→star fly-to feel like a snap because the camera covered
most of the trajectory in the first second (factor=12 puts
~50% of motion in the first 35% of time, defeating the
perceptual "lift-off" / "warp" / "arrival" phases). Round-5
(2026-05-06) closes the divergence and aligns with Gaia per
`feedback_default_gaia_fidelity.md` (when stuck between match
Gaia and atlas-opinion, pick Gaia). At factor=60 the curve
puts ~30% of the duration in stall at each end and the
trajectory is concentrated in [t=0.3, t=0.7] — a clear
cinematic departure → warp → arrival profile.

#### S3 — Two-channel `StellarFlightTransition` (~2h)

New class in `src/lib/camera/StellarFlightTransition.ts` with
separate position and orientation channels. API:

```ts
interface StellarFlightSpec {
  startPos: Vector3; // current camera world position
  endPos: Vector3; // landing position (from S1's
  // computeAtlasFlightLanding)
  startTarget: Vector3; // current OrbitControls target
  endTarget: Vector3; // star world position
  posDurationMs: number;
  oriDurationMs: number;
  posEasing?: (t: number) => number; // default logisticSigmoid
  oriEasing?: (t: number) => number; // default logisticSigmoid
  onComplete?: () => void;
}

class StellarFlightTransition {
  start(spec: StellarFlightSpec): void;
  /** @returns { position, target, done } each frame. */
  update(): { position: Vector3; target: Vector3; done: boolean } | null;
  /** Freeze both channels at current alpha and emit the
   *  intermediate state for the controller to read. */
  cancel(): { position: Vector3; target: Vector3 } | null;
}
```

**Why target instead of quaternion** (per S4's OrbitControls
coordination strategy, picked option 2): Three.js `OrbitControls`
derives camera orientation from `(camera.position, controls.target)`
and overwrites any direct quaternion change in its per-frame
`update()`. Animating quaternion directly would fight the
controls. Instead, the orientation channel outputs the lerped
`target` position; `OrbitControls.update()` then derives the
camera's actual orientation from it. The camera ALWAYS looks at
`controls.target` during the transition, which is exactly the
desired behavior (look at the destination star throughout
fly-to). No quaternion math needed at this layer.

Position interpolates a straight line `mix(startPos, endPos,
posEasing(alpha))` — no Bézier, no sun-avoid. Target
interpolates `mix(startTarget, endTarget, oriEasing(alpha))`.
Two alphas clamped against two durations; `done` is true when
both reach 1. `cancel()` freezes both at the current alpha so
S5's interrupt path can emit the intermediate values cleanly
(no snap-back, no jump-forward). Default easing is
`CameraTransition.logisticSigmoid` (S2).

#### S4 — `CameraController` HYG branch refactor (~2.5h)

Rewrite `setupCameraHyg()` ([`CameraController.tsx:230-317`](../../src/components/canvas/CameraController.tsx)).

**OrbitControls coordination strategy** (per Codex 2026-05-05
correction): Three.js `OrbitControls` derives camera orientation
from `(camera.position, controls.target)`. Animating
`camera.quaternion` directly via slerp would FIGHT
`OrbitControls` (which would re-derive the orientation from
`target` every frame and overwrite the slerp). Two valid
approaches:

1. **Suspend `OrbitControls` during the transition**, animate
   the camera's `quaternion` directly with slerp, then on
   completion compute a `controls.target` consistent with the
   final orientation and re-enable controls. Clean separation
   but adds a brief window where user input is ignored.
2. **Animate `controls.target` per frame** in lockstep with
   the camera position, so `OrbitControls`-derived orientation
   is the slerp result. The animated target follows a path
   from "current target" to "the focused star", easing on the
   orientation duration (independent of the position duration).
   `OrbitControls.update()` then produces the correct camera
   orientation each frame.

**Pick (2)** for minimum churn — no input suspension, the
existing `OrbitControls` damping continues to read sensible
values throughout. The tradeoff: orientation is constrained to
"look at controls.target", which is exactly what we want
(camera ALWAYS looks at the destination star during fly-to).

Concrete steps:

- Drop the `controls.target.copy(targetPos)` snap at
  [`CameraController.tsx:315`](../../src/components/canvas/CameraController.tsx).
- Compute landing distance via
  `computeAtlasFlightLanding(radiusWu, distancePc)` from S1.
  Apply body-clearance floor: final distance =
  `Math.max(radiusWu * 1.1, result.distanceWu)`.
- Compute scale-aware durations:
  ```ts
  const linearDist = startPos.distanceTo(endPos);
  const posDurationMs = clamp(Math.log10(linearDist + 1) * 1500, 2000, 8000);
  const angularSweep = (current target → star target) angular distance;
  const oriDurationMs = clamp(angularSweep * 2000, 1000, 4000);
  ```
- Update `near`/`minDistance` from the REAL star radius (not
  the `HYG_FOCUS_DEFAULT_RADIUS_WORLD` placeholder still used
  at
  [`CameraController.tsx:485-490`](../../src/components/canvas/CameraController.tsx)).
  Per Codex 2026-05-05: HYG fly-to currently falls back to
  the placeholder; M2.5 wires the real radius through.
- Hand off to `stellarFlightTransition.start(spec)`. Per-frame
  `useFrame` reads `update()`, applies the position lerp to
  `camera.position`, and the orientation lerp to
  `controls.target` (which `OrbitControls.update()` then
  consumes to derive the camera's actual quaternion).

#### S5 — Real interrupt with state preservation (~45min)

`OrbitControls "start"` event currently only flips
`isFlying = false`
([`CameraController.tsx:461-468`](../../src/components/canvas/CameraController.tsx))
— it does NOT cancel the transition or preserve intermediate
state. Result: when the user mouse-drags mid-flight, the camera
jerks because the position transition keeps running but the
target snap already happened on focus-set.

Real interrupt:

- Add `stellarFlightTransition.cancel()` which freezes both
  channels at their current alpha and emits the current state
  `{ position, target }` for the controller to read.
- `OrbitControls "start"` calls `cancel()`, reads the frozen
  state, and updates `controls.target` to its frozen value
  WITHOUT resetting it. The position channel stops driving
  `camera.position` so the user's drag input takes over
  smoothly.
- Test: simulate a mouse-drag at alpha=0.4 during a fly-to;
  the camera position must stop animating and the target must
  retain the alpha=0.4 interpolated value (not snap forward to
  the destination).

#### S6 — Mesh pre-warm sync (~30min, glue with M3)

`HygStellarMesh` already mounts `<ProceduralSun3D>` when
`meshActive` flips. M2.5 ensures the gate fires DURING the
transition, not at the end: pass an estimated arrival distance
to the gate so the mesh starts rendering in the last 25-30% of
the position transition. M3 (cross-fade) consumes this
naturally — the fade window opens during arrival.

This may also reveal a coupling fix in `HygStellarMesh.tsx` —
if so, scope to a small extra commit, not into M2.5 proper.

#### S7 — Tests + smoke (~30min)

- Pure-function unit tests for S1 (`stellarFlightSolidAngle`) and
  S2 (`CameraTransition.logisticSigmoid`) — already shipped with
  S1 + S2 commits.
- `StellarFlightTransition` lifecycle tests — start/update/cancel
  state machine with vitest fake timers (already shipped with S3
  commit `ccd7d2f`). NOTE: S3 explicitly avoids quaternion math
  (animates `controls.target` per frame; OrbitControls derives
  the camera quaternion). The original S7 line referencing a
  "quaternion endpoint test" was a leftover from the pre-Codex
  draft and is removed in this revision.
- S5 interrupt regression: simulate `OrbitControls "start"`
  during a fly-to and verify no orientation snap-back, no
  position jump (math-only test on the controller hook plus a
  Playwright e2e if reproducible at HMR-stable cadence).
- Visual smoke: focused Sirius from boot solar-system view;
  pixel-diff comparison of arrival pose against a baseline
  screenshot.
- User-driven smoke: 4 named stars × 2 zoom cycles, declared
  PASS only after user confirms "feels seamless".

### Acceptance

1. **No orientation snap**: clicking a HYG star → camera rotates
   smoothly via the animated `controls.target` (no
   pre-animation `target.copy()`); position translates in
   parallel on its own duration. No "spin-then-glide" feel.
2. **Adaptive landing size, mesh-spawn-safe**: Sirius (2.6 pc)
   lands at ~1° angular radius (~456 wu from the star);
   Betelgeuse (197.8 pc) at ~0.93° angular radius (~254,000 wu).
   For stars beyond ~2003 pc the landing falls back to the Atlas
   mesh-spawn floor (5e-3 rad angular radius) — same as pre-M2.5
   - so the procedural mesh ALWAYS spawns on arrival regardless
     of catalog distance. Numbers updated post Codex round-3
     angular-radius semantic fix (was 913/509k/1200pc when the
     lib halved Gaia's curve, the 2× landing-distance regression
     C-1 caught).
3. **Gate-driven physics arrival (Round-6 update, 2026-05-06)**:
   the position channel is no longer duration-driven — it is a
   gate-driven physics integrator (`HygPhysicsFlight`) that
   terminates when the apparent angular radius reaches the
   adaptive target. With the R6-F first-guess calibration
   (`MAX_VELOCITY_FACTOR = 3.0`, cap-bound effective decay rate
   3.0/s) Sirius arrives in ≈4.65 s; Betelgeuse in ≈1.7 s; the
   far-edge crossover anchor in ~5 s. The orientation channel
   stays duration-driven via `OrientationLerp` (extracted from
   `StellarFlightTransition`'s orientation half) with the
   round-5b angular-sweep heuristic. Independent completion
   conditions: position is gate-driven, orientation is duration-
   driven; the global `flyingRef.isFlying` flag flips to false
   only when BOTH are inactive. The pre-Round-6 round-4 C-6
   parsec-scale formula is preserved in
   `StellarFlightTransition` for future scripted-tour features
   (per Round-6 acceptance §5) but no longer drives click-driven
   focus.
4. **Mid-flight interrupt is REAL**: user mouse-drag during
   transition freezes both channels at their current alpha;
   `controls.target` retains the interpolated value;
   `camera.position` stops animating. No orientation
   snap-back, no position jump.
5. **`near`/`minDistance` use real radius**, not the
   `HYG_FOCUS_DEFAULT_RADIUS_WORLD` placeholder. Pin in test:
   a focused white-dwarf-class star (R≈0.0465 wu) gets
   `near` ≈ `0.05 × 0.01 = 5e-4 wu`, not the default.
6. **Sun-at-origin and curated-body fly-to byte-identical**:
   the existing `setupCamera()` non-HYG branch is untouched.
   Pin via test that bodies still use the existing
   `transitionRef.current.start(...)` path, not the new
   `stellarFlightTransition`.
7. **No mesh-spawn regression**: a focused HYG star at 2805 pc
   (or any distance beyond the Gaia/Atlas crossover ~2003 pc
   post-round-3) still spawns the procedural mesh on arrival.
   Pin via the S1 test pinning `clampedByAtlasFloor` behavior
   plus a runtime smoke at a far-catalog-edge star.
8. **User smokes 4 named stars × 2 zoom cycles** — mix of
   close (Sirius, Proxima) and distant (Betelgeuse, plus one
   ≥500 pc star) — confirms "feels seamless and the mesh is
   visible at every focus" before T6.4 core ships.

### Scope discipline

This is Gaia-informed for the math + transition contract;
atlas-native for the mesh-spawn clamp, the OrbitControls
coordination, and the interrupt path. Diff-gate applies to
the Gaia-borrowed math + lerp + easing factor; per-decision
rationale applies to the Atlas-specific glue (clamp, target-
animation strategy, interrupt). Per
`feedback_default_gaia_fidelity.md`, when stuck between "match
Gaia" and "atlas opinion", pick Gaia — UNLESS doing so breaks
an existing Atlas contract (the mesh-spawn gate is the canonical
example here, hence the clamp).

NOT in scope: reworking the curated-body fly-to path (still
uses solar-system-tuned `PrivilegedPosition` math; fine where
it is). NOT in scope: refactoring `OrbitControls` itself.
NOT in scope: ~~porting Gaia's interactive `go_to_object`
continuous-push mode~~ — **SUPERSEDED 2026-05-06 by Round-6
below.** The original M2.5 framing ("REPL/scripting use case,
not the focus-click flow Atlas needs") was the diagnosis trap
L41 captures: atlas read `go_to_object` as a scripting tool
because Gaia exposes it via the script API, but the user-
perceived click flow IS structurally that physics-push model,
not the `transition_position` lerp atlas wired in M2.5. See
§Round-6 for the active port. NOT in scope: a full AAA
navigation rewrite — M2.5 is specifically the HYG flight
contract that makes M3 (cross-fade) meaningful.

### Codex round-3 (2026-05-05) — hotfix plan

After S7 shipped, Codex (`gpt-5.5`) reviewed the full M2.5
diff and surfaced 4 findings. Per
`feedback_codex_findings_toward_1to1.md` the agent verified
each against `/tmp/gaiasky` source before accepting; all 4
hold. The hotfix lands as a single commit
`fix(t6.4): M2.5 — Codex round-3 review (4 findings)`.

#### C-1 (P1) — `stellarFlightSolidAngle.ts` angle-math mis-port

**Source verification**:
`/tmp/gaiasky/core/src/gaiasky/script/v2/impl/InteractiveCameraModule.java:172`
does `if (focusView.getSolidAngle() < target)`.
`/tmp/gaiasky/core/src/gaiasky/scene/component/ParticleSet.java:1220`
defines `getSolidAngle = (size / distance) / camera.getFovFactor()`.
That return is angular-radius-like, not full angle (small-angle
approx: angular radius ≈ size/distance). The misleading
`solidAngle` name actually returns the small-angle equivalent
of an angular radius, normalised by fovFactor.

**Atlas mis-port**: lib reads `1.0°` from the curve, names it
`fullAngle`, then halves it to `angularRadius = 0.5°` — but the
Gaia curve was already angular radius. The downstream
`computeFlightTargetDistance` uses `radius / tan(fullAngle * 0.5)`,
which is `radius / tan(0.5°)` — exactly half what the contract
implies (`radius / tan(1.0°)`). Sirius lands at ~916 wu when
the contract says ~458 wu (2× regression).

**Fix**:

- Rename `computeGaiaTargetFullAngleRad` →
  `computeGaiaTargetAngularRadiusRad`.
- Rename `AtlasFlightTarget.fullAngleRad` →
  `angularRadiusRad`. (The existing `angularRadiusRad`
  diagnostic field collapses into the renamed primary.)
- Drop the `gaiaFullRad * 0.5` step in
  `computeAtlasFlightTarget` — the curve is already angular
  radius.
- Change `computeFlightTargetDistance` from `radius / tan(angle * 0.5)`
  to `radius / tan(angularRadius)`.
- Update tests in `stellarFlightSolidAngle.test.ts` — the
  Sirius landing pin moves from ~913 wu to ~458 wu, the
  crossover pc-distance moves from ~1200 pc to **~2003 pc**
  (Gaia angular-radius values are 2× the prior halved-as-
  full-angle interpretation, so the crossover with the 5e-3 rad
  Atlas floor moves OUTWARD on the lerp curve, not inward —
  the round-3 spec's "~600 pc" claim was a sign error; the
  actual round-4 audit correction is ~2003 pc, which the lib
  header `computeAtlasFlightTarget` doc and the tests both
  pin), and the regression-vs-pre-M2.5 ratio recomputes
  (~3.5×, not ~1.74×).
- Header note: Atlas does NOT replicate Gaia's
  `getFovFactor()` normalisation. Round-4 audit corrected the
  reference numbers: Gaia `TAN_REF_FOV = tan(20°)` (reference
  fov 40°, NOT 60°), config default `fov: 45.0`, fovFactor at
  the shared-default fov ≈ 1.138 (NOT ≈ 1). Atlas accepts the
  ~14% landing-distance inflation as scope of "Gaia-informed".
  See `stellarFlightSolidAngle.ts` header for the full note.
- **Helper already in place** (carried over before the
  hotfix): `resolveHygDistanceFromSunPc(starIndex, catalog)`
  lives in `src/lib/focus/hygFocusResolver.ts` with 5 unit
  tests in `hygFocusResolver.test.ts` (axis-aligned,
  off-axis, Sirius-like, error-case, rotation-invariance).
  `setupCameraHyg` already calls it (the inline parsec
  computation was replaced). The hotfix just needs to read
  from this helper as-is — no more inline `Math.sqrt(px²+
py²+pz²)` to refactor. Helper + tests + call site landed
  carrying the work from a parallel session in worktree
  `claude/blissful-khayyam-58f719` (commit `ec6ebac`,
  discarded — only this clean refactor was salvaged).

#### C-2 (P1) — `HygStellarMesh.tsx` pre-warm hides sprite

**Problem**: S6's force-activate at `progress >= 0.70` flips
`meshActiveRef.current = true`, which the
`useEffect [scene, starIndex, meshActive]` then writes as
`skipMask = 1`, suppressing the sprite for the focused star.
Without M3's cross-fade, the procedural mesh is still
angularly small at progress 0.70 (the natural gate
`sa > ENTER` would only fire close to landing). Net visual:
sprite gone, mesh tiny — exactly the gap M2.5 was meant to
avoid.

**Fix**: revert the force-activate. Keep the `getHygFlightPosProgress`
import + the singleton + the `posProgressRaw` getter on
`StellarFlightTransition` — they're cheap, production-inert,
and M3 will consume them. The mesh activation reverts to
purely sa-driven, identical to pre-S6 behaviour. `S6` shipped
the channel; M3 will ship the consumer.

#### C-3 (P2) — Honest comments about Gaia parity

**Source verification**:
`/tmp/gaiasky/core/src/gaiasky/script/v2/impl/CameraModule.java:1419-1424`
does `qd.set(startOrientation).slerp(endOrientation, ...)`,
then `cam.setUp(up)` and `cam.setDirection(direction)`. Real
quaternion slerp on the dir+up channel. Atlas lerps
`controls.target` and lets `OrbitControls.lookAt` derive
orientation — drops the Gaia explicit up/roll channel.

**Fix**: edit comments in `StellarFlightTransition.ts`
header and the `setupCameraHyg` call site in
`CameraController.tsx` to call this an "OrbitControls-native
approximation". Reference the Gaia source line. State the
divergence explicitly: Atlas accepts orientation that's
constrained to "look at controls.target", which is exactly
what we want for HYG focus-click but is NOT the same as the
Gaia free-camera quaternion slerp.

#### C-4 (P2) — Stronger e2e via test-only window hooks

**Problem**: `e2e/hyg-focus.spec.ts` only asserts focus
survives + console clean. The flight-contract regressions
(orientation snap, landing distance off, mesh activation
timing, interrupt behaviour) all pass through this spec
silently.

**Fix** — same pattern as `__ATLAS_TEST_STORE__` (gated on
`__ATLAS_TEST_FREEZE__`, production-inert):

- New `window.__ATLAS_TEST_CAMERA__` — getter that returns
  `{ position: {x,y,z}, target: {x,y,z}, quaternion: {x,y,z,w} }`
  by reading the R3F root state. Wired up in `Scene.tsx` (or a
  small `<TestCameraProbe>` component) when the freeze flag is
  set; reads via `useThree(s => s.camera)` + the `OrbitControls`
  ref.
- New `window.__ATLAS_TEST_MESH_STATE__` — getter that returns
  `{ meshActive: boolean, skipMaskAtIndex(K: number): number }`.
  Wired in `<HygStellarMesh>`'s body when freeze flag set —
  exposes `meshActiveRef.current` plus a closure over the
  scene to read `findStarfieldSkipMask(scene)`.
- `e2e/hyg-focus.spec.ts` extensions:
  1. **Target lerped, not snapped**: capture
     `__ATLAS_TEST_CAMERA__().target` at t=0.5s after focus
     dispatch, again at t=2s, again at t=10s. Assert the
     three samples are distinct and trend monotonically
     toward the star world position.
  2. **Landing distance bracket**: at t=10s (post-fly),
     compute `|camera.position − star_world_pos|`. Assert
     in `[400, 1000] wu` for Sirius (the post-C-1 angular-
     radius math gives ~458 wu; bracket gives slack).
  3. **Mesh active at landing**: assert
     `__ATLAS_TEST_MESH_STATE__().meshActive === true` at
     t=10s.
  4. **skipMask in sync with meshActive**:
     `skipMaskAtIndex(0) === 1` at t=10s; pre-fly assertion
     that it's 0 before the dispatch.
  5. **No mid-fly skipMask flip until natural gate**: sample
     `skipMaskAtIndex(0)` at t=4s (raw alpha ~0.5) — should
     still be 0, since C-2 reverted the force-activate.

**Hand-off note**: Playwright local runs default to headless;
if any of the new assertions are flaky from a state-timing
perspective, the user can run `npx playwright test
e2e/hyg-focus.spec.ts --headed --project=chromium` to debug.

#### Wave-file consistency sweep

After the code lands, also:

- Downgrade "Gaia-faithful" claims in this M2.5 section to
  "Gaia-informed" where the contract diverges (the C-3
  honest-comments principle, applied to the wave file
  itself).
- Add new lessons.md entry **L39**: "Codex 'verified against
  source' claims must distinguish 'name from source text'
  from 'semantics matched against actual consumer site'.
  Gaia's `getSolidAngle()` returns
  `(size/distance)/fovFactor` despite the name suggesting
  steradians; mis-porting the semantic cost a 2× landing-
  distance bug that no gate caught for 4 sub-step commits."
  Long form goes to a new `tasks/archive/postmortems/`
  entry if needed; lessons.md keeps the rule short per L38.

#### Acceptance for the hotfix commit

1. All 4 findings fixed (C-1 through C-4).
2. `npm run test:run` passes — Sirius landing pin updated,
   crossover-distance pin updated, force-activate test removed
   (or marked as M3-pending), all other math tests still pass.
3. `npm run lint` clean. `npm run build` clean. `npm run docs:check`
   clean.
4. Playwright `e2e/hyg-focus.spec.ts` passes with the new
   assertions on the local machine.
5. Runtime smoke (preview MCP): boot → click HYG star → fly-to
   completes → procedural mesh visible at arrival → console
   clean (only pre-existing GLSL X4122/X4008 warnings).

---

## M3 — Smooth sprite ↔ mesh cross-fade ✅ DONE 2026-05-06

**Effort**: ~2-3h.
**Scope tag**: atlas-native.
**Status**: shipped 2026-05-06.
**Depends on**: M2.5 PASS (smooth fly-to is a prereq for the
fade to be meaningful — a janky arrival makes any fade look
like a hack).

Replace `a_skipMask` Float32 0/1 binary with `a_fadeAlpha`
Float32 [0..1] continuous. Sprite multiplies its alpha by
`(1 - a_fadeAlpha)`. Mesh's `uVisibility` ramps 0→1 in parallel
to `a_fadeAlpha` 0→1. Hysteresis still gates direction of the
ramp (sa > ENTER → ramp toward 1, sa < EXIT → ramp toward 0);
cushion prevents oscillation. Fade duration ~300ms (or smoothstep
over the small ENTER..EXIT solidAngle band).

### Threshold coordination — focused-star-specific (per Codex round-3 P2)

The LEN0/ENTER 1:17 ratio gap was diagnosed for the FOCUSED star
path. Globally lowering `u_LEN0` or raising `STELLAR_MESH_ENTER_RAD`
would affect EVERY sprite + EVERY mesh activation in the catalog
(perf regression risk — LEN0 controls hard-zero of sprites at
very small angular sizes; mesh ENTER affects gate timing for the
entire HYG focus mechanism).

Instead: keep LEN0 + ENTER global constants unchanged; introduce
a focused-star-specific fade window. HygStellarMesh tracks a
ramp progress in a ref (0..1, increments toward 1 when
`solidAngle > ENTER`, decrements toward 0 when `< EXIT`, smooth
over ~300 ms). Per-frame writes `a_fadeAlpha[K] = ramp` for the
focused star K only — all other sprites stay at 0 (current
behavior). Mesh `uVisibility = ramp` in parallel.

Pin invariant in test: focused sprite alpha never reaches zero
before mesh ramp ≥ ε for some small ε; equivalently,
`(focused-sprite alpha + mesh visibility) > 0` throughout the
transition.

**Acceptance**: zoom-in to HYG star shows continuous sprite-to-
mesh visual transition with no pop, no gap, no flicker. User
smokes 2 zoom cycles per named star.

**Shipped**:

- Renamed `starfieldSkipMask.ts` → `starfieldFadeAlpha.ts`
  (helper + tests). Float32 [0..1] semantics now: 0 = sprite
  full, 1 = sprite fully suppressed. Renamed
  `buildSkipMaskAttribute` → `buildFadeAlphaAttribute`.
- `Starfield.tsx` vertex-shader rewrite: replaced
  `if (a_skipMask > 0.5) alpha=0` binary cutoff with
  `alpha *= clamp(1.0 - a_fadeAlpha, 0.0, 1.0)` continuous
  attenuation. Attribute renamed from `a_skipMask` →
  `a_fadeAlpha` (instanced 1-component Float32). Quad-null
  branch retained for the post-fade-completion case (alpha
  ≤ 1e-3) so tiny / fully-faded sprites still skip the
  fragment shader.
- `ProceduralSun3D.tsx`: new optional `visibilityRef:
MutableRefObject<number>` prop. When wired (HygStellarMesh
  path), the per-frame useFrame reads `ref.current` (clamped
  [0..1]) and writes to all four materials' `uVisibility`
  uniforms in lockstep with the sprite-side `a_fadeAlpha`.
  When omitted (Sun mount), `uVisibility` stays at its init
  value 1.0 — pre-M3 behaviour byte-identical.
- `HygStellarMesh.tsx` rewrite: replaced the binary
  `meshActive` flip + `writeSkipMask(K, 0|1)` pattern with a
  ramp ref (`rampRef.current` ∈ [0..1]) + linear integrator.
  Per-frame useFrame:
  - Computes solid angle from camera distance + star radius
  - Hysteresis gate `shouldStellarMeshBeActive` decides
    target = 1 (when sa > ENTER) or 0 (when sa < EXIT)
  - `stepRampToward(rampRef.current, target, delta, 300ms)`
    advances the ramp linearly toward the target
  - Writes `a_fadeAlpha[K] = rampRef.current` to the
    Starfield instanced attribute
  - Component mounts/unmounts on the 0↔(0,1] boundary
    (state `meshActive = rampRef.current > 0`) so React
    only re-renders at the start/end of a fade, never per
    fade tick.
  - Passes `visibilityRef={rampRef}` to ProceduralSun3D so
    the mesh's visibility uniform tracks the same ramp.
- New `hygMeshFadeRamp.ts` (factor of `stepRampToward` out of
  `HygStellarMesh.tsx` for Fast Refresh + independent
  testability).
- `e2e/hyg-focus.spec.ts`: probe shape updated
  (`skipMaskAtIndex` → `fadeAlphaAtIndex`). Pre-fly = 0,
  post-landing = 1 — same steady-state contract, now
  reached via the 300 ms cross-fade instead of an instant
  flip.

**Tests**:

- `starfieldFadeAlpha.test.ts` (9 tests, was 7): default-zero,
  continuous-value mutation, distinct buffers, empty input,
  InstancedBufferAttribute integration + `needsUpdate` version
  bump, plus M3-specific design-pin (sprite alpha
  multiplier `(1 - x)` + mesh visibility `x` always sum to 1).
- `hygMeshFadeRamp.test.ts` (9 tests, new): integrator math
  — single-step snap, linear traversal, symmetric directions,
  clamp [0..1], 60 fps tick stream settles at exact 300 ms,
  no overshoot on direction reversal, `durationMs ≤ 0`
  defensive path, zero-dt holds value, sum invariant
  preserved at every tick.

**Boot smoke (Preview MCP)**: dev mode boots clean, R3F canvas
mounts, search → Sirius click + HygStarPanel render works.
No NEW console errors from M3. (The pre-existing
"orbital engine failed" telemetry on HYG focus is unchanged
— Sidebar's stale `useOrbitalCalculation` call on `hyg:K`
IDs predates M3 and is already spawned as a follow-up
task.)

**Visual user-smoke**: deferred to user (only the user can
verify "no pop" perceptually). The sum-invariant test pin
(sprite + mesh = 1 at every tick) is the strongest
machine-checkable proxy.

---

## M4 — Class variation in stellarVisualProfileFrom (perceptual distinction)

**Effort**: ~6-9h (was ~2-4h pre-2026-05-05 spec revision; expanded
after live diagnostic of "todas amarelas" symptom + Codex pre-M4
review pulled the scope toward spectral-color-first).
**Scope tag**: atlas-native (visual identity is an atlas decision;
Gaia Sky's `starsurface.fragment.glsl` is a separate hardcoded
shader, not a port target).
**Status**: pending.
**Depends on**: M3 PASS (so transition smoothness is verified
before changing per-star visual identity).

### Diagnostic driving this revision (2026-05-05)

After M1+M2 shipped and HYG meshes started rendering, the user
observed every focused star renders the same yellow-orange disc
regardless of spectral class (Sirius A1V should be blue-white,
Betelgeuse M2Iab should be deep red supergiant, Proxima M5.5V
should be red dwarf — all looked solar). Source trace confirms
the gap:

1. **`stellarVisualProfileFrom`** ([stellarPhysics.ts:510-537](../../src/lib/stellarPhysics.ts))
   spreads `SUN_DEFAULT_VISUAL_PROFILE` and overrides only 3 of
   28 fields: `surfaceBrightness` (clamped to [0.4, 1.5] — too
   tight to drive perceived class difference), `raysHue` (offset
   ±0.4), `flaresHue` (same offset). The other 25 fields stay
   solar.
2. **`absmag` is accepted by the input but ignored** —
   `StellarPhysicsInput.absmag?` is declared but
   `stellarVisualProfileFrom` never reads it (and `radiusFromSpect`
   only consults absmag when `spect` is also present, falling
   back to `1.0` otherwise per [stellarPhysics.ts:367-369](../../src/lib/stellarPhysics.ts:367)).
3. **`brightnessToColor` is the dominant color source** in BOTH
   sphere ([proceduralSunShaders.ts:245](../../src/components/canvas/shaders/proceduralSunShaders.ts:245))
   AND glow ([proceduralSunShaders.ts:382](../../src/components/canvas/shaders/proceduralSunShaders.ts:382))
   fragments: `vec3(b, b², b⁴) * uBrightness`. `uTint` only
   modulates the relative magnitudes of the G/B channels but
   the formula is monotonic across channels — it CANNOT produce
   blue-dominant output regardless of `uTint`. So per-class
   color simply doesn't fit through this formula.
4. **All materials use `toneMapped: false`** ([ProceduralSun3D.tsx:409,448,483,523,565](../../src/components/canvas/ProceduralSun3D.tsx:409))
   — shader output is linear-RGB HDR; tone map is downstream.
   Any new color uniform MUST be in linear-RGB, NOT sRGB.

Conclusion: M4 cannot succeed by varying just the existing 28
profile fields. The hardcoded `brightnessToColor` formula must
yield to a per-class color uniform; the descriptor reaching
`stellarVisualProfileFrom` must include `absmag` + `luminosityClass`;
and granulation must split temperature-derived character (hue,
contrast tilt) from luminosity-class-derived structure (cell size,
time scale).

### Spec (revised post-Codex review)

#### S1 — Typed visual descriptor (TS-side, ~1h)

Replace ad-hoc `StellarPhysicsInput` shape with an explicit
`StellarVisualDescriptor` interface bundling everything the
visual profile actually needs:

```ts
interface StellarVisualDescriptor {
  tEff: number; // Kelvin — primary color driver
  spectralClass: SpectralClass; // O / B / A / F / G / K / M / L / T / Y / WD
  luminosityClass: LuminosityClass; // Ia / Ib / II / III / IV / V / VI / VII (or null)
  bv: number; // B-V color index — fallback for tEff
  absmag: number | null; // V-band absolute magnitude — drives glow intensity
  radiusSolar: number; // R/R_sun — informational + glow scaling
}
```

`luminosityClass` is the new field. Today `parseSpectralClass`
extracts it from spect strings like "G2V" or "M2Iab" but
`stellarVisualProfileFrom` ignores it. M5 (spect-fallback) needs
to surface it too.

Helper `descriptorFromCatalog(spect, bv, absmag)` builds the
descriptor from raw HYG fields; consumers use the descriptor.

#### S2 — Blackbody linear-RGB color (TS-side, ~1-2h)

Add `blackbodyRgbFromTemperature(tEff: number): [r, g, b]` in
linear-RGB space. Use a published table (e.g. CIE 1931 chromaticity
of Planck radiator → sRGB primaries → undo sRGB transfer) or a
piecewise polynomial fit (Krystek 1985 / Mitchell Charity table).
Pin per-named-star outputs in tests:

- Sun (5778 K) → close to current `brightnessToColor(1.0)` output
  in linear space. Pin numeric tolerance, NOT byte-identical
  (changing the formula breaks byte-identical; perceptually
  identical post-tone-map is the real requirement).
- Sirius (~9940 K) → blue-white tilt
- Betelgeuse (~3500 K) → deep orange-red
- Proxima (~3050 K) → reddish-orange (not deep red — M5.5V is
  cooler than Betelgeuse but per blackbody curve, not by much in
  the visible-band sense; deep-red perception is from low
  luminosity, not pure tEff)

Math lives in `src/lib/stellarColor.ts` (new file). Pure function,
fully unit-tested before reaching the shader.

#### S3 — Shader uniforms for class color (~1h)

Replace `brightnessToColor`'s color decision with a per-class
RGB uniform passed in linear space.

Sphere fragment proposed form:

```glsl
uniform vec3 uClassColor;  // linear-RGB blackbody, T6.4-M4
uniform float uClassWhitePoint; // brightness above which the
                                // surface saturates to white
                                // (HDR core look). Solar default
                                // matches current ~5.0.

vec3 brightnessToColor(float b) {
  // Lerp class hue to white at high brightness so the core stays
  // HDR-white (current visual character) while the limb / lower-
  // brightness regions show class color. Replaces the prior
  // (b, b², b⁴) formula which only spanned warm-yellow→white.
  vec3 hue = uClassColor;
  float saturation = 1.0 - smoothstep(1.0, uClassWhitePoint, b);
  vec3 chroma = mix(vec3(1.0), hue, saturation);
  return chroma * b * uBrightness;
}
```

Sphere AND glow must take the same uniform — otherwise the corona
color drifts from the surface. JS-side: `uClassColor` is set once
per focus change (not per-frame) since the descriptor is stable
while a star is focused.

Sun-default test vector: with `uClassColor = blackbodyRgb(5778)`,
the post-tone-map pixel-diff against the pre-M4 baseline must be
≤1% (roughly 1 sRGB unit per channel after the post-process
pipeline). This is the realistic substitute for byte-identical;
pin via Playwright pixel-diff in the named e2e spec.

#### S4 — `absmag` participates in glow intensity (~30min)

`absmag` is the V-band luminosity proxy. Hot-luminous stars
(Sirius A1V at M_V ≈ +1.4, Rigel B8Ia at M_V ≈ -7.8) should have
visibly brighter halos than equally-hot lower-luminosity stars.
Map `absmag` to a `glowBrightness` multiplier:

```ts
glowBrightnessScale = clamp(
  pow(10, -0.4 * (absmag - M_SUN_V_ABS) * 0.15),
  0.5,
  3.0
);
```

The 0.15 exponent damps the actual luminosity ratio so the visual
range stays usable (a true 10^(-0.4 \* ΔM) spans ~10^9 across the
catalog). Clamp [0.5, 3.0] keeps even ultra-faint M dwarfs visible
and ultra-bright supergiants from blowing out bloom. Pure
artistic factor — explicitly NOT a physics claim about coronal
luminosity.

#### S5 — Granulation: temperature × luminosity class (~2-3h)

Two-axis split:

- **Temperature** drives granulation **contrast** + **noise hue
  bias** (cool stars have stronger thermal contrast between cells
  and intercellular lanes; hot stars have radiative atmospheres
  with weak granulation visibility). Map `granulationContrast`
  and (optionally) a small surface-color modulation per cell.
- **Luminosity class** drives **cell size** (`spatialFreq`) and
  **time scale** (`temporalFreq`). Pressure scale height H_p
  scales as `~T_eff / g`; cell size scales with H_p. Supergiants
  (Ia/Ib/II) have huge H_p (low gravity at extended atmospheres)
  → very large cells, slow turnover (months on Betelgeuse vs
  ~5 min on Sun). Main-sequence (V) → cells matched to T_eff.
  White dwarfs (VII) → high g, tiny cells if any.

Approximate mapping (atlas-opinion, not physics):

| Class             | spatialFreq | temporalFreq | contrast |
| ----------------- | ----------- | ------------ | -------- |
| Ia (supergiant)   | 1.5         | 0.02         | 0.45     |
| III (giant)       | 4           | 0.06         | 0.35     |
| V (main sequence) | 6           | 0.10         | 0.25     |
| VII (white dwarf) | 12          | 0.20         | 0.10     |

Hot main-sequence (O / B / A) gets contrast scaled down by
~`exp(-(tEff - 6000) / 4000)` to flatten the surface — radiative
atmospheres look smoother. Pin per-named-star output in tests.

#### S6 — Glow / rays / flares as class+activity art (~1h)

Frame as art-direction layer, not physics:

- **Glow `falloffColor` + tint**: O/B → cleaner, sharper falloff;
  K/M → softer, broader halo (chromospheric activity look).
  Driven by `tEff` curves, not blackbody dictates.
- **Rays amplitude**: low for hot main-sequence (clean look); higher
  for K/M dwarfs (active corona); slow + wide for supergiants.
  Scale by both class AND `absmag` so a luminous K giant has a
  proportionally larger ray field than a faint K dwarf.
- **Flares amplitude**: pronounced for M dwarfs (Proxima-like
  activity); minimal for hot main-sequence; smoothly slow for
  supergiants. Same dual-driver as rays.

NO claim about "coronal temperature" or "X-ray emission color".
This is purely visual character tuning so the four named stars
read as distinct without overstating physics.

#### S7 — Test pinning (~1h)

Named-star byte-pin in `stellarPhysics.test.ts`:

```ts
const expectedDescriptors = {
  Sun: { tEff: 5778, spectralClass: "G", luminosityClass: "V", absmag: 4.83, ... },
  Sirius: { tEff: 9940, spectralClass: "A", luminosityClass: "V", absmag: 1.42, ... },
  Betelgeuse: { tEff: 3500, spectralClass: "M", luminosityClass: "Ia-Iab", absmag: -5.85, ... },
  Proxima: { tEff: 3050, spectralClass: "M", luminosityClass: "V", absmag: 15.49, ... },
};
```

Plus `blackbodyRgb` numeric tolerance pins, plus pixel-diff e2e
spec (`e2e/hyg-focus.spec.ts`, new) for the four stars at a fixed
camera state.

### Acceptance

1. **Spectral color is the dominant perceptual driver**: at fixed
   focus state, Sirius reads as blue-white, Betelgeuse as
   orange-red, Proxima as warm-orange, Sun as solar.
2. **Sun-at-origin perceptual identity**: post-tone-map pixel-diff
   ≤1% per channel against the pre-M4 baseline screenshot. NOT
   byte-identical pre-tone-map (the formula change breaks that;
   the user-visible result is what matters).
3. **Luminosity class drives texture**: Betelgeuse vs an M2V
   reference shows visibly different granulation cell scale
   (Betelgeuse: huge slow cells; M2V dwarf: smaller cells with
   activity).
4. **`absmag` participates in glow brightness**: Rigel (B8Ia,
   M_V≈-7.8) shows a noticeably brighter halo than Sirius (A1V,
   M_V≈+1.4) despite Sirius being hotter. Pin via screenshot
   comparison.
5. **No coupling bugs**: SUBAGENT VERIFY confirms changing one
   star's descriptor doesn't bleed into another's render.
6. **User smokes** 4 named stars × 2 zoom cycles, each visibly
   distinct, before T6.4 core ships.

### Scope discipline

Per `feedback_default_gaia_fidelity.md`: when a decision branches
"Gaia parity" vs "atlas opinion", pick Gaia. Here Gaia uses a
hardcoded shader with no class variation (`starsurface.fragment.glsl`
viewport=1500x750, frequency=80) — there's no Gaia-side reference
to match. M4 is fully atlas-opinion; the goal is _perceptual_
class distinction users recognize, not numerical agreement with
any external sim. Explicit rationale comments per atlas-native
divergence.

---

## M5 — Spect-missing fallback via absmag

**Effort**: ~2-3h (Path B + Path A combined).
**Scope tag**: atlas-native.
**Status**: pending.
**Depends on**: independent of M1-M4 (data fix), can run in
parallel; but easier to user-smoke after M4 so visual identity
is tied together.

### Premise correction (per Codex round-3 P2)

Original M5 plan claimed `radiusFromSpect(null, absmag)`
"already supports absmag fallback" — **false**.
`stellarPhysics.ts:369` does `if (!spect) return 1.0;` BEFORE
looking at absmag. The absmag parameter is consumed only by the
optional Stefan-Boltzmann refinement path (line ~402+) which
runs AFTER class-table lookup, i.e., only when spect IS present.
With spect="", absmag is never read.

M5 must do real work, not extend an existing path.

### Path A — runtime absmag fallback (~1-2h)

Extend `radiusFromSpect(spect, absmag)` to accept the
missing-spect case. When `spect` is null/empty AND `absmag` is
finite, derive an estimated radius from absmag alone using a
B-V-augmented Stefan-Boltzmann path (atlas already has B-V at
HygStellarMesh call site, can pass it through; or compute T_eff
from B-V via `temperatureFromBV` and combine with absmag for
L/L_sun then `R/R_sun = sqrt(L) × (T_sun/T_eff)²`). This handles
ANY star with missing spect, not just named ones. Pin per-named-
star radius assertions: Betelgeuse, Antares, Rigel, Proxima from
absmag+B-V should produce supergiant-class radii within ~30% of
catalog literature values.

### Path B — preserve named stars at canonicalization (~1h)

Fix `scripts/build-hyg-binary.js` `capSpectByFrequency` so it
always keeps the canonical class of any star in
`hyg-v1.names.json` (allowlist by HIP/proper-name/index — note:
the names sidecar today carries `hip` + `proper` + `bayer` +
`flam` + `con` + `index`; HD field is NOT exposed there, so
allowlist must use the available keys). Re-bake the 4 tier
`.bin` files. T6.2-β-β already showed how to re-bake; M5-B is a
delta. Net effect: Betelgeuse/Antares/Rigel get their real spect
strings restored in the binary; existing `radiusFromSpect(spect)`
does the right thing without runtime fallback work. Costs an L32
PNG re-bake flag (binary file change → screenshots if any depend
on starfield rendering — but they don't).

### Recommended: Path B + Path A combined

Path B fixes the specific-named-star regression that drove M5;
Path A is a defensive runtime fallback for any unnamed star
whose canonical class still ends up empty.

**Acceptance**: Betelgeuse renders supergiant-sized (~887 R_sol)
not Sun-sized. Same for Antares (~680 R_sol), Rigel (~78 R_sol),
Proxima (~0.15 R_sol). Pin per-star radius assertions in
`stellarPhysics.test.ts`.

---

## M6 — HygStarPanel + Search + Wikipedia integration (forward-port from round-5b)

**Effort**: ~14h across 8 sub-tracks.
**Scope tag**: atlas-native (UX), Gaia-informed (Wikipedia
integration matches `DataInfoWindow.java` pattern, not a 1:1
port of the Java `GenericDialog`).
**Status**: pending, **promoted from OPTIONAL** to **active forward-port**
after user smoke 2026-05-05 surfaced two related gaps: search
box doesn't find HYG stars (U-2 in STATUS Carryover) and no
info panel for clicked stars (U-5).

### Scope evolution (2026-05-06)

Original M6 plan (Codex round-3 reframe): atlas-native local
panel only, ~2-3h, OPTIONAL post-recovery polish. User smoke
2026-05-05 escalated this to **a real UX gap** that surfaces
EVERY time the user focuses a HYG star — there's no signal
about WHAT they're looking at. User explicitly requested
Wikipedia-style info pull (matching Gaia's behavior).

Verified against `/tmp/gaiasky/core/src/gaiasky/gui/window/DataInfoWindow.java`:
Gaia uses **Wikipedia REST API** (free, no key, rate-limited
to 1 req/s):

- `URL_WIKI_API_SUMMARY = https://$LANG.wikipedia.org/api/rest_v1/page/summary/{title}`
  → returns `extract` (~250 chars summary) + `thumbnail` URL
- `URL_WIKI_REDIRECT = .../w/api.php?action=query&titles=$NAME&redirects`
  → resolves "Sirius A" → "Sirius" (canonical title)
- `URL_WIKI_LANGLINKS = .../w/api.php?action=query&prop=langlinks&titles=$NAME`
  → finds article's localized version (en → pt-BR fallback)
- Disambiguation by suffix: `Sirius_(star)` tried before
  `Sirius` to avoid namespace collisions (e.g. `Mercury` planet
  vs element vs car)
- User-Agent header: Wikipedia API ToS requires identification
  (Gaia uses `"GaiaSky/{version} (https://gaiasky.space)"`)
- 1s minimum spacing between requests, 15s timeout, AbortController
  on focus change (cancel stale fetch when user clicks elsewhere)

### Spec — 8 sub-tracks

#### Sub-track A — i18n foundation (~2h) ✅ DONE 2026-05-06

**Why first**: D (HygStarPanel) needs translation hooks. Worth
laying the foundation cleanly even if only D consumes it for
now; future polish work migrates other strings to the same
infrastructure.

**Shipped**:

- Deps `react-i18next` + `i18next` + `i18next-browser-languagedetector`
  added to `package.json` dependencies.
- `src/i18n/index.ts` — singleton init guarded with
  `i18n.isInitialized` (idempotent across HMR re-runs); enables
  `LanguageDetector` only in browser env; pins `lng: "en"` in
  node so vitest doesn't depend on `navigator`.
- `src/i18n/locales/en/common.json` + `src/i18n/locales/pt-BR/common.json`
  with `hygStarPanel.{title, fields.*, units.*, wikipedia.*}`
  keys covering the panel + Wikipedia states D/E will consume.
- `src/main.tsx` wires the side-effect import `import "./i18n";`
  before `createRoot()` (no `<I18nextProvider>` needed; the
  default i18next singleton is what `useTranslation` resolves).
- `src/i18n/i18n.test.ts` — 9 tests pinning init, language flip,
  fallback chains, and locale-key parity. Smoke verified
  pt-BR → "Carregando…", "fr" → fallback "Loading…", "pt" bare
  resolves to pt-BR bundle, "en-US" resolves to en.

**Divergence from spec resolved during ship**:

- Spec called for `nonExplicitSupportedLngs: true` to make bare
  "pt" resolve to pt-BR. In i18next v26 this option REJECTS the
  matching regional resource (sets `i18n.languages = ["en"]` and
  `t()` returns English even after `changeLanguage("pt-BR")`).
  Removed it. Default i18next behavior already resolves "pt" →
  "pt-BR" and "en-US" → "en" with just `supportedLngs:
["en", "pt-BR"]` + `fallbackLng: "en"`. Pinned via test.

**Files touched**:

- `package.json` + `package-lock.json` — 3 new deps.
- `src/i18n/index.ts` (new).
- `src/i18n/locales/en/common.json` (new).
- `src/i18n/locales/pt-BR/common.json` (new).
- `src/i18n/i18n.test.ts` (new).
- `src/main.tsx` (+1 line — side-effect import).

**Codex round-1 audit absorbed (P3 × 2, 2026-05-06)**:

- **Detection contract made explicit** (`src/i18n/index.ts`).
  Implicit `i18next-browser-languagedetector` defaults included
  `cookie` (reads `document.cookie["i18next"]`),
  `sessionStorage`, and `htmlTag` paths atlas does not use.
  Replaced with explicit `detection: { order: ["querystring",
"localStorage", "navigator"], caches: ["localStorage"],
lookupQuerystring: "lng", lookupLocalStorage:
LANGUAGE_STORAGE_KEY }`. Shrinks cross-tab influence
  surface (a stray `i18next` cookie elsewhere on the host
  no longer overrides the user's choice) and pins the
  cache key sub-track G's settings toggle will target.
  `LANGUAGE_STORAGE_KEY = "i18nextLng"` exported.
- **Language-state assertions added** (`src/i18n/i18n.test.ts`).
  Existing tests asserted `t()` outputs but not the resolved
  language chain. Added five tests pinning `i18n.language`,
  `i18n.languages[0]`, and `i18n.resolvedLanguage` after each
  changeLanguage. `i18n.languages[0]` is the canary that
  exposed the removed `nonExplicitSupportedLngs` regression
  during the original ship — pinning it prevents future
  regressions where `t()` happens to return the right string
  via fallback while the resolution chain has silently broken.

**M6-A Codex audit closed**: 0 P0/P1/P2 findings; both P3
findings absorbed. Test count 9 → 15.

Today's v2 binary carries `positions`, `colorIndices`,
`magnitudes`, `spectIndices`, `spectStrings`, `absmag`. The HYG
CSV source has `proper`, `bayer`, `flam`, `hd`, `hip`, `gl`
(Gliese), `con` — all needed for human-readable identification

- search.

**Shipped**:

- `src/utils/hygBinary.ts` rewrite: `HYG_VERSION = 3`,
  `HYG_VERSION_V2 = 2` added, `HYG_VERSION_V1 = 1` retained;
  separate v1 / v2 / v3 parser branches with default-fill of
  v3 fields for older buffers; v3 encoder with five string
  pools (`spect` / `properName` / `bayer` / `constellation`
  / `gliese`); `clampUint{8,16,32}` helpers gating the v3
  numeric IDs. v3 record = 38 bytes; v3 header = 36 bytes.
  String index widths: uint8 for `spect` / `bayer` /
  `constellation` (≤256 entries); uint16 for `properName`
  (~365 unique in HYG) / `gliese` (~3.8k unique).
- Option (b) per Codex round-5b: HYG abbreviations stored
  as ASCII (`bayer="Alp"`); UI layer maps to Greek glyph at
  display time (sub-track D's responsibility).
- `src/utils/hygBinary.test.ts` rewritten (35 tests): v3
  round-trip, Sirius designation pin (proper="Sirius",
  bayer="Alp", constellation="CMa", gliese="Gl 244",
  flamsteed=9, hd=48915, hip=32349), v3 designation
  flag-bit set/clear semantics, uint8/uint16 pool overflow
  rejection, v2 + v1 backward-compat parsing with v3
  default-fill, error paths (corrupt string table, mismatched
  size, unknown version, wrong magic).
- `scripts/build-hyg-binary.js` extended: reads `hd` + `gl`
  CSV columns; emits v3 fields to encoder; tier rebake
  produced 109,400 stars (low/medium/high/full) at the new
  per-star size. Sidecar `hyg-v1.names.json` shape preserved
  (3,380 named entries).
- `src/lib/focus/hygFocusResolver.test.ts` mock helper updated
  to default-fill the new `HygCatalogData` v3 fields under
  TS strict mode.

**Re-baked artifacts** (committed in same commit as the format
bump per the rebake protocol):

- `public/data/hyg-stars/hyg-v1-low.bin{,.gz}` — 500 stars
- `public/data/hyg-stars/hyg-v1-medium.bin{,.gz}` — 10,000 stars
- `public/data/hyg-stars/hyg-v1-high.bin{,.gz}` — 50,000 stars
- `public/data/hyg-stars/hyg-v1-full.bin{,.gz}` — 109,400 stars

Full tier raw 4.00 MB, gzip 3.10 MB (vs v2's ~2.7 MB raw / ~1.5
MB gzip). The 65% size increase is the cost of carrying full
designations end-to-end without a sidecar fetch round-trip.

**Boot smoke**: dev server boots clean, R3F canvas live, zero
console errors, hyg:0 (Sirius) parses with all designations
matching the test pin.

**Codex round-1 audit absorbed (P3 × 3, 2026-05-06)**:

- **String encoding switched from byte-mask to UTF-8** (`src/
utils/hygBinary.ts`). M6-B's first ship documented strings
  as "ASCII" but used a single-byte mask
  (`charCodeAt(i) & 0xff`), which round-tripped 0x00–0xFF
  cleanly but corrupted any codepoint > 0xFF. A CSV audit found
  two real HYG names (`Bibhā` and `Sāmaya`, both with U+0101)
  decoded as `Bibh<SOH>` / `S<SOH>maya` on disk. Replaced the
  pool encoder with `TextEncoder.encode` and the decoder with
  `TextDecoder("utf-8")`; the uint8 length prefix now counts
  bytes, not codepoints. ASCII-only pools (spect / bayer /
  constellation / gliese) are byte-identical before/after; only
  the proper-name pool grew (~30 bytes total across 15 affected
  Latin-1 + 2 multi-byte names). New encoder rejects any entry
  whose UTF-8 encoding exceeds 255 bytes.
- **Sentinel validation enforced in `readStringPool`** (`src/
utils/hygBinary.ts`). Format spec said every pool starts with
  the "" sentinel, but the parser previously accepted size-
  consistent buffers with `tableBytes === 0` (returning `[]`)
  or a non-empty leading entry — a hand-crafted v3 buffer could
  silently break consumers that index `pool[0]`. Added an
  explicit length-≥-1 + `pool[0] === ""` check; throws
  `HYG <pool> table corrupt: missing leading "" sentinel` on
  violation. Applies to v2 (spect) + v3 (all five pools).
- **Raw-byte offset assertions added** (`src/utils/hygBinary.
test.ts`). Existing round-trip tests would have passed if
  encoder + parser drifted to the same wrong offsets. Added a
  describe block that encodes `SIRIUS_V3` and reads raw
  `DataView` bytes at offsets 16/20/24/28/32 (header table-size
  fields) and 23/25/26/27/29/30/34 (per-star designation
  extension) so a typo on either side fails the test
  independently.
- **Re-baked tier files** (committed in same commit as the
  encoding fix). Verified `Bibhā` + `Sāmaya` + `Tupã` + `Mönch`
  - `Lusitânia` all round-trip in the new binaries. Tier file
    sizes essentially unchanged (~30 extra bytes spread across
    4 tiers); `low` 23,277 → 23,335 B, `full` 4,194,128 →
    4,194,302 B.

**M6-B Codex audit closed**: 0 P0/P1/P2 findings; all three P3
findings absorbed. The first finding (P3-1) was arguably
understated — `Bibhā` / `Sāmaya` corruption was active data
loss, not a hypothetical concern. Test count 35 → 42.

**Naming + encoding corrections (Codex round-5b audit, 2026-05-06)**:

- Constants are `HYG_VERSION` (currently 2) and `HYG_VERSION_V1`
  (= 1) in `src/utils/hygBinary.ts:82,87`. There is **no**
  `HYG_BINARY_FORMAT_VERSION`. Bump `HYG_VERSION = 3`, add a new
  `HYG_VERSION_V2 = 2`, retain `HYG_VERSION_V1 = 1`. The parser
  already has explicit branches for v1 and v2 (lines 204, 207);
  add a v3 branch that reads the new fields, keep both v1 and v2
  paths intact for back-compat with already-baked local binaries.
- Current string-pool encoding is single-byte (Latin-1-ish):
  `view.setUint8(offset, s.charCodeAt(i) & 0xff)`
  (`hygBinary.ts:504`) and `String.fromCharCode(view.getUint8(...))`
  (`:320`). NOT UTF-8. Greek `α` (codepoint 0x03B1) gets masked
  to 0xB1 (`±`) on encode and never round-trips. v3 has two
  options: (a) **switch to UTF-8** via `TextEncoder`/`TextDecoder`
  with explicit byte-length prefixes per string, OR (b) **store
  the raw HYG abbreviation** (`bayer="Alp"` for Sirius per the
  HYG CSV — NOT `bayer="α"`) and translate to Greek at display
  time via a small abbreviation→Greek map in the UI layer.
  Option (b) is the smaller diff and matches how the HYG CSV
  actually stores the field. **Recommended**: option (b) for v3
  minimum-diff; revisit (a) only if a future field needs full
  Unicode (e.g. localized star names from Wikipedia langlinks).

Extend `scripts/build-hyg-binary.js` to emit:

- `properName: string[]` (sidecar, ~80% empty per HYG; ASCII
  proper names like "Sirius", "Vega" — already ASCII-clean)
- `bayer: string[]` (HYG abbreviation: "Alp", "Bet", "Gam", ...
  — NOT Greek letters; UI maps to Greek for display)
- `flamsteed: number[]` (constellation-relative numbering;
  0 = missing)
- `hd: number[]` (HD catalog ID; 0 = missing)
- `hip: number[]` (Hipparcos ID; 0 = missing)
- `gliese: string[]` (Gliese catalog ID; "" = missing — ASCII
  like "Gl 244")
- `constellation: string[]` (3-letter abbrev like "CMa" — ASCII)

Re-bake the 4 tier `.bin` files from updated
`scripts/build-hyg-binary.js`. T6.2-β-β documented the rebake
protocol; M6-B is a delta.

Tests: parser round-trip pinning each new field for at least
Sirius (hyg:0 — proper="Sirius", bayer="Alp", con="CMa",
hd=48915, hip=32349). v1 + v2 fixtures stay in the test suite
as back-compat regression pins.

#### Sub-track C — SearchBar wires HYG name index (~1.5h) ✅ DONE 2026-05-06

Today's `SearchBar.tsx` searches `BODIES_BY_ID` (curated
solar-system bodies). Extend to ALSO match the HYG catalog by
proper name + Bayer + HD/HIP/Gliese.

**Shipped**:

- `src/lib/starfield/hygNameIndex.ts` (new) — builds a
  `Map<lowercased-key, starIndex[]>` over a parsed
  `HygCatalogData`. Multi-key per the spec:
  - `properName.toLowerCase()`
  - `"<bayer-abbrev> <con>"` (e.g. `"alp cma"`)
  - `"<bayer-greek> <con>"` (e.g. `"α cma"`) — second key
    via the new `BAYER_TO_GREEK` table so users can type
    either the HYG abbreviation OR the IAU Greek glyph
  - `"hd <id>"` / `"hip <id>"`
  - `gliese.toLowerCase()` (e.g. `"gl 244a"`)
    Cached via module-level `WeakMap<HygCatalogData,
HygNameIndex>` so re-runs against the same parsed catalog
    return the cached structure (catalog ref changes → rebuild).
- `searchHygCatalog(query, catalog, limit)` returns
  `HygSearchResult[]` ranked by score (exact > prefix >
  word-prefix > substring) with brightness as tie-breaker.
  Result fields cover the rich panel D will display:
  `properName`, `bayerAbbrev` + `bayerGreek`, `constellation`,
  `spect`, `hd`, `hip`, `gliese`, `distancePc`, `mag`,
  `matchedField`.
- `normalizeHygQuery` strips diacritics (Tupã → tupa,
  Bibhā → bibha) so the pt-BR keyboard path doesn't need
  composed accent input. Greek glyphs survive NFD
  decomposition on basic letters.
- `src/components/ui/SearchBar.tsx` extended:
  - Subscribes to the tier-bound HYG catalog via
    `useStarfieldCatalog` (same hook CameraController uses).
  - Computes `hygResults` via `useMemo` (top 5).
  - Renders a "Stars (HYG)" section divider above the HYG
    rows in the listbox; keyboard nav indexes the unified
    flat result array, section header is `aria-hidden`.
  - On select, branches by hit kind: curated bodies stay on
    the existing `selectId(body.id)` path; HYG matches call
    `selectId(formatHygFocusId(starIndex))` so the camera
    fly-to + mesh mount fire through the same code path
    Round-6 wired.
  - Empty-state message updated to mention star designation
    examples ("Sirius" / "α CMa" / "HD 48915").
- `src/components/ui/SearchBar.test.tsx` — added 3 HYG-match
  tests using a `vi.spyOn(starfieldModule, "getCachedHygCatalog")`
  fixture. Pins (1) "Stars (HYG)" section appears for a
  matching query; (2) clicking a HYG result dispatches a
  `hyg:K` focus ID; (3) Greek-glyph form `α CMa` resolves to
  the same star.
- `src/lib/starfield/hygNameIndex.test.ts` (new, 26 tests)
  — covers the spec pin (Sirius via Sirius / siri / alp cma /
  α CMa / Α CMA / HD 48915 / hip 32349 / Gl 244A), index
  caching, tie-break by magnitude, diacritic strip, empty
  query handling, matched-field tagging.

**Boot smoke — manual search-bar exercise**: Preview MCP,
search panel opened, typed:

- `Sirius` → `"Sirius · A0 · 2.64 pc · HYG"` row appears
- `α CMa` → same Sirius row
- `HD 48915` → same Sirius row
- `Vega` → `"Vega · A0V · 7.68 pc · HYG"` row

Zero console errors during the exercise.

#### Sub-track D — HygStarPanel UI (~2h) ✅ DONE 2026-05-06

New component analogous to existing `Sidebar` but driven by
`hyg:K` focus IDs.

**Shipped**:

- `src/components/ui/HygStarPanel.tsx` (new) — React panel that
  reads `selectedId` from the store, gates render on
  `parseHygFocusId(focusId) !== null`, subscribes to the same
  tier-bound HYG catalog SearchBar / CameraController use, and
  derives all displayed fields from the parsed v3 binary +
  stellar-physics helpers. Renders parallel to existing `Sidebar`
  (mounted in `Overlay.tsx`); Sidebar stays HYG-blind for curated
  bodies.
- `src/lib/starfield/hygStarInfo.ts` (new) — `buildHygStarInfo`
  builder + `HygStarInfo` shape. Lives outside the panel
  component file so the Fast Refresh rule
  (react-refresh/only-export-components) stays clean and the
  formatting heuristics stay independently testable.
- `src/lib/stellarPhysics.ts` (+71 lines) — new
  `massFromSpectAbsmag(spect, absmag)` helper. Class-aware mass
  estimate: main-sequence uses the standard mass-luminosity
  relation (M = L^(1/3.5) on absmag); giants (III) / bright
  giants (II) / supergiants (Ia/Ib/I) use class-typical
  fixed values; white dwarfs default to 0.6 M_sun. Returns NaN
  when class + absmag don't yield a usable estimate (panel hides
  the field).
- `src/i18n/locales/{en,pt-BR}/common.json` — extended with
  `hygStarPanel.{closeLabel, unknown, fields.designation,
wikipedia.heading}` keys.
- `src/store.ts` — new `wikipediaIntegrationEnabled: boolean`
  (default `true`) + `setWikipediaIntegrationEnabled` setter.
  Intentionally NOT in the persist `partialize` until sub-track
  G ships, so a missing localStorage entry doesn't read as
  "false" on a fresh boot.
- `src/components/ui/Overlay.tsx` (+1 line) — mounts
  `<HygStarPanel />` adjacent to `<Sidebar />`.

**Panel content**:

- Header row: primary name (proper > Bayer-Greek + con > Bayer
  - con > Flamsteed + con > HD > HIP > Gliese > "Star #idx")
    plus a designation strip listing every alternate identifier
    separated by `·`.
- Stellar physics grid (each row hidden when its source is
  missing): spectral class, effective temperature (round to
  K, locale-formatted thousand-separator), radius in solar
  units, mass in solar units, distance (pc + ly), apparent
  magnitude, absolute magnitude, constellation.
- Wikipedia "About" section (skipped entirely when
  `wikipediaIntegrationEnabled` is false): loading skeleton →
  ready (extract truncated to 250 chars on a clean word
  boundary, lazy-loaded thumbnail max 240×240 px, "Read more"
  link with `target="_blank" rel="noopener noreferrer"`) /
  empty / error (with retry button that reruns the fetch).
- Close button mirrors `Sidebar`'s — clears `selectedId` to
  null.

**Tests**:

- `src/lib/starfield/hygStarInfo.ts` — unit tests via the
  panel test (covered by `buildHygStarInfo` describe block).
- `src/components/ui/HygStarPanel.test.tsx` (13 tests):
  visibility gates (hidden when no focus, hidden when curated
  body), Sirius-fixture full render (primary name + designation
  - spectral class + distance regex + constellation), Wikipedia
    states (loading skeleton, ready with link attrs, empty,
    error + retry path), settings toggle disables the section,
    close button clears focus.

**Boot smoke (manual exercise via Preview MCP)**: in pt-BR
locale, opened search → typed "Sirius" → clicked the HYG row
→ `hyg:0` dispatched. Panel rendered:

- "Estrela" (Portuguese title)
- "Sirius" with designation "α CMa · 9 CMa · HD 48915 · HIP 32349 · Gl 244A"
- "Classe espectral: A0", "Temperatura efetiva: 9.900 K"
- "Raio: 1.66 × Sol", "Massa: 2.43 M☉"
- "Distância: 2.64 pc · 8.60 al", "Magnitude aparente: -1.40"
- "Constelação: CMa"
- "Sobre" section pulled the live pt.wikipedia.org "Sírio" article
  with extract + thumbnail + "Ler mais na Wikipédia" link
  Zero console errors during the exercise.

- New `src/components/ui/HygStarPanel.tsx`
- Fields displayed (each with i18n key):
  - Common name (sidecar `proper`/`bayer`/`flam`/`con`)
  - Spectral class (parsed) + raw spect string
  - Effective temperature (Ballesteros + MK refinement)
  - Radius in R_sol (with Sun-comparison "× Sun")
  - Mass estimate from class + absmag (rough heuristic)
  - Distance in parsec / light-year
  - Apparent + absolute magnitude
  - Constellation
  - Wikipedia section (consumed from sub-track E):
    - Loading skeleton (1s)
    - Article extract (250 chars truncated, 12px font)
    - Thumbnail (max 240×240px, lazy-loaded)
    - "Read more on Wikipedia" link (target=`_blank`,
      `rel="noopener noreferrer"`)
    - Empty state ("No Wikipedia article found")
    - Error state ("Couldn't load info, retry?" with retry
      button)
- Render slot: parallel to existing Sidebar, gated on
  `parseHygFocusId(focusId) !== null`. Sidebar stays HYG-blind
  for curated bodies.
- Settings toggle wiring: read `wikipediaIntegrationEnabled`
  from store; if false, skip the Wikipedia section entirely
- Tests: testing-library renders panel with mocked data, asserts
  all sections exist; mocks Wikipedia client to assert error /
  empty / success states render correctly

#### Sub-track E — Wikipedia REST client (~3h)

New `src/lib/wikipedia/wikipediaClient.ts`. Standalone module,
reusable beyond M6 (e.g. future curated-body panels).

**Browser-context corrections (Codex round-5b audit, 2026-05-06)**:
Atlas runs in a browser, not a JVM, and the Wikipedia surface
behaves differently from the libGDX HTTP context Gaia targets.
The plan must reflect:

- **`User-Agent` is NOT settable from `fetch()` in browsers** (the
  browser sends its own UA). The Wikipedia/MediaWiki guidelines
  for client-side calls allow identification via the
  `Api-User-Agent` request header instead. Atlas SHOULD set
  `Api-User-Agent: "atlas-orbital/{pkgJson.version} (https://github.com/<...>/atlas-orbital; contact email)"`
  per <https://www.mediawiki.org/wiki/API:Cross-site_requests>
  and <https://foundation.wikimedia.org/wiki/Policy:Wikimedia_Foundation_User-Agent_Policy/en>.
- **MediaWiki Action API requires `origin=*`** in the query
  string for unauthenticated cross-origin requests. Endpoints
  affected: `URL_WIKI_REDIRECT` and `URL_WIKI_LANGLINKS`. Without
  `origin=*` the response lacks `Access-Control-Allow-Origin`
  and the browser blocks it. The REST API endpoint
  (`api/rest_v1/page/summary/{title}`) sends CORS headers by
  default and does NOT need `origin=*`.
- **Wikipedia uses 2-letter language codes** (`en`, `pt`, `es`,
  ...). `pt-BR.wikipedia.org` does not exist. Map `pt-BR → pt`,
  `en-US → en`, etc., before composing the URL.

**Rate-limit math fix (Codex round-5b audit, 2026-05-06)**:
Gaia's `DataInfoWindow.java:153-161` does
`Thread.sleep(now - lastTrackedRequest)` — that's the **elapsed**
time, which is wrong (sleeping for elapsed=200ms when 1000ms is
required = total spacing 400ms, still under the limit). Atlas's
client must sleep for **remaining** time:
`Math.max(0, REQUEST_WAIT_MS - (now - lastRequestStartedAt))`.
The "matches Gaia exactly" framing in the original plan is
abandoned for this reason. Document the deviation in the
client header. Also note: client-side rate limiting only
serializes **per tab/module instance**; it cannot enforce
Wikimedia-wide or per-IP limits across users. That is acceptable
for Atlas's interactive single-user workflow but should not be
described as a defense against Wikimedia-side abuse.

API:

```ts
interface WikipediaSummary {
  title: string;
  extract: string;
  thumbnailUrl: string | null;
  pageUrl: string;
  language: string;
}

interface WikipediaClient {
  fetchSummary(
    query: string,
    options?: {
      /** App-level locale, e.g. "pt-BR" or "en". Mapped to
       *  Wikipedia's 2-letter code internally. */
      lang?: string;
      /** Suffix variants tried in order, e.g. ["_(star)", ""]. */
      suffixes?: string[];
      signal?: AbortSignal;
    }
  ): Promise<WikipediaSummary | null>;
}
```

Implementation:

- Internal request queue serializing to ≥1000 ms between request
  starts (Atlas-native math — sleeps the **remaining** wait, not
  the elapsed)
- 15 s per-request timeout via `AbortController.timeout` /
  manual setTimeout-then-abort (matches Gaia's intent;
  AbortController's `AbortSignal.timeout(ms)` works in modern
  browsers)
- Disambiguation: try `query + suffix` for each suffix in order;
  first 200-OK response wins, all 404s = null
- Headers: `Api-User-Agent` (NOT `User-Agent`) identifying the app
- Action API URL composition includes `&origin=*` for
  cross-origin compliance
- Lang-code mapping: `pt-BR → pt`, default `en` if unmapped
- AbortController-aware: if `options.signal` fires mid-request,
  cancel the underlying fetch and reject with `AbortError`
- Langlinks fallback: if user lang is pt and only en page exists,
  fetch the en summary; if user lang is pt and pt page exists,
  prefer pt
- Tests: mocked fetch with sequenced 200/404/timeout responses;
  assert serialization (sleep-remaining math), abort, suffix
  disambiguation, langlinks fallback, lang-code mapping
  (pt-BR → pt URL)

**Shipped 2026-05-06** ✅:

- `src/lib/wikipedia/wikipediaClient.ts` (new) — `createWikipediaClient`
  factory + default `wikipediaClient` singleton + bound
  `fetchSummary` export. Each instance owns its own serialization
  queue + last-request timestamp so tests start clean.
- `mapLanguageCode(lang)` — Atlas locale → Wikipedia 2-letter code
  (`pt-BR → pt`, `pt-PT → pt`, `en-US → en`, `es-AR → es`,
  case-insensitive). Default `"en"` for empty / non-string input.
- `fetchSummary(query, options)`:
  - Suffix disambiguation: tries each suffix in order, default
    `["_(star)", ""]`. First 200 wins; all 404 → null. Disambiguation
    page (`type === "disambiguation"`) and missing-extract responses
    also collapse to null.
  - Single-step en fallback: if user lang is non-English and all
    suffixes 404 there, retry the same suffixes at `en.wikipedia.org`.
    Simpler than walking the Action API langlinks endpoint
    (which would need `&origin=*`); for our use case the only
    fallback target is `en` anyway.
  - `Api-User-Agent` header set (NOT `User-Agent` — browsers
    silently strip it). Default identifier: `atlas-orbital/0.1.0
(https://github.com/fgferre/atlas-orbital)`.
  - Per-tab serialized request queue with **sleep-remaining**
    math (`max(0, requestWaitMs - elapsed)`). Codex round-5b
    correction over Gaia's `Thread.sleep(elapsed)` in
    `DataInfoWindow.java:153-161` documented in the file header.
  - 15 s per-request timeout via hand-rolled
    `createCombinedAbort` helper (timer + caller-signal forwarding
    on a single `AbortController`). Hand-rolled instead of
    `AbortSignal.any([...])` so vitest's fake timers can drive
    both legs deterministically.
  - Caller `signal` aborts the in-flight fetch + the queue wait
    phase; rejects with the signal's `reason` (or `AbortError`).
- `src/lib/wikipedia/wikipediaClient.test.ts` (new, 24 tests):
  - `mapLanguageCode` (5 tests): explicit Atlas→Wikipedia maps,
    regional fallback, case-insensitivity, default behavior.
  - basics (6 tests): 200 OK parse, URL encoding (spaces → `_`,
    `encodeURIComponent`), disambiguation → null, missing-extract
    → null, empty / whitespace query → null without firing fetch,
    non-200 / non-404 throws.
  - Suffix disambiguation (2 tests): first-match wins, all-404
    returns null.
  - Language mapping + en fallback (4 tests): pt-BR → pt URL, pt
    404 → en succeeds with `language: "en"`, en-only does NOT
    re-try fallback, both pt + en 404 → null.
  - Request serialization (4 tests): first-request fires
    immediately, two concurrent requests serialize 1000 ms apart,
    sleep-remaining math (300 ms elapsed → 700 ms wait, not 300),
    no extra spacing when previous request finished long ago.
  - Abort + timeout (3 tests): pre-aborted signal throws without
    firing fetch, in-flight abort propagates to fetch + rejects,
    15 s timeout fires `TimeoutError` DOMException.

**Boot smoke** (Preview MCP, no UI consumer yet — module ships in
the bundle for D's future consumption): React + R3F mount, zero
console errors, build chunk includes wikipediaClient.

#### Sub-track F — IndexedDB persistent cache (~1.5h) ✅ DONE 2026-05-06

Wikipedia summaries don't change frequently; caching them between
sessions saves bandwidth + latency.

**Shipped**:

- `src/lib/wikipedia/wikipediaCache.ts` (new) — `createWikipediaCache`
  factory + module-default `wikipediaCache` singleton. Each
  instance owns one DB connection (lazy: opens on first call).
  IndexedDB schema:
  - DB name `atlas-wikipedia-cache` (version 1)
  - Object store `summaries`, keyed on `${lang}:${title}` strings
  - Index `fetchedAt` for the LRU sweep
  - Value shape `{ summary: WikipediaSummary, fetchedAt: number }`
- TTL: 30 days. `get()` returns null when `now - fetchedAt > ttlMs`
  so the client re-fetches and overwrites with a fresh `fetchedAt`.
- Eviction: count-bounded LRU at 200 entries. After each `set` the
  count is checked; if over the cap, the cursor walks the
  `fetchedAt` index in ascending order and deletes the oldest
  entries until back under (the eviction runs in the same
  transaction as the put, so concurrent puts never see a transient
  over-cap state externally). The "L" is "least recently fetched":
  `set` updates `fetchedAt`; `get` does NOT (no read-side touch).
- Optional dependency injection: `dbName`, `storeName`, `ttlMs`,
  `maxEntries`, `nowImpl`, `openDbImpl` — used by tests for
  isolation + clock control.
- `clear()` + `close()` helpers for tests + a future "purge cache"
  affordance.

**Wired into `wikipediaClient`** (`src/lib/wikipedia/wikipediaClient.ts`):

- New `cache?: WikipediaCacheLike` field on `WikipediaClientConfig`.
  Tests omit it (no caching path); the default singleton wires
  the module-default `wikipediaCache` so every read across the
  app benefits transparently.
- `fetchSummaryAtLang` reads cache BEFORE the rate-limit gate so
  hits don't pay the 1-second per-tab spacing.
- Cache miss → falls through to network. Successful network
  result is fire-and-forget written back to the cache; the
  promise's `.catch` swallows write errors so a slow / broken
  IDB never delays the returned summary.
- Cache get rejection (IDB blocked / quota / corrupted) is also
  swallowed — treated as a miss, client falls through to
  network. The client must remain usable when the cache is
  unavailable.
- 404 / disambiguation / no-extract responses do NOT write to
  cache (we don't want to remember "no result" entries that
  could be transient).

**Tests**:

- `src/lib/wikipedia/wikipediaCache.test.ts` (new, 14 tests) —
  uses `fake-indexeddb/auto` polyfill so vitest's node env can
  exercise the real DB shim:
  - Round-trip: missing key returns null, set + get matches,
    lang-scoped keys stay separate, overwrite, clear.
  - TTL: within-TTL returns entry, past-TTL returns null,
    re-set after staleness refreshes fetchedAt.
  - LRU: at-cap retains all, over-cap evicts oldest, re-set
    saves an entry from eviction, multi-evict on a single set
    when count is over by more than one.
  - Entry shape: fetchedAt reflects the configured clock,
    WikipediaSummary preserved verbatim.
- `src/lib/wikipedia/wikipediaClient.test.ts` (+5 tests):
  cache hit returns without fetch, miss falls through + writes
  back, no write on 404 / disambiguation, cache get rejection
  treated as miss, cache set rejection doesn't break the
  request.

**Boot smoke (Preview MCP, manual exercise)**: pt-BR locale
(navigator.language="pt-BR" → mapped to `pt`), opened search,
typed "Sirius", clicked the HYG row. After ~4s the panel
rendered the live `pt.wikipedia.org` "Sírio" article. Read
IndexedDB directly and confirmed:

- DB `atlas-wikipedia-cache` exists with object store `summaries`
- One entry with key `"pt:Sirius"`
- Entry value: `{ summary: { title, extract, language: "pt", ... },
fetchedAt: <number> }`
  Zero console errors during the exercise.

#### Sub-track G — Settings toggle (~1h) ✅ DONE 2026-05-06

User-controllable opt-out for Wikipedia integration.

**Shipped**:

- Store: `wikipediaIntegrationEnabled` (already added in M6-D
  with default `true`) added to the persist `partialize` in
  `src/store.ts` so the toggle survives page reloads. Field
  also added to `PersistedSlice` in `src/store.persistMigration.ts`
  (no version bump — backward-compatible field; envelopes
  predating this commit get the default `true` via `coerceToV1`).
- UI: new "Integrations" section in `src/components/ui/GearPopover.tsx`
  between "About" and "Developer". One toggle row using the
  existing inline switch pattern (matches the Developer section's
  Debug Logging style):
  - `role="switch"` + `aria-checked` reflecting the store
  - "Wikipedia about-text" label + "On"/"Off" status
  - Helper line: "Pulls a brief summary + thumbnail from Wikipedia
    for the selected star (HYG focus). Off ⇒ no network requests,
    no cache writes."
- Persist key: atlas's existing centralized `atlas-orbital-store`
  key (PERSIST_KEY) rather than a separate
  `atlas.settings.wikipediaIntegrationEnabled` slot. Deviation
  from the wave-file spec — atlas's persist convention is one
  consolidated envelope; a separate key would create surface
  inconsistency vs all other persisted state. The functional
  goal ("toggle persists") is satisfied either way.

**Tests**:

- `src/store.persistMigration.test.ts` (+2 tests): partial v1
  envelope without the field defaults to `true`; explicit
  `wikipediaIntegrationEnabled: false` survives the round-trip.
- `src/components/ui/GearPopover.test.tsx` (+3 tests): switch
  reflects current store value; click flips store + UI; click
  again flips back ON.

**Boot smoke** (Preview MCP, manual exercise): opened gear menu
→ clicked "Wikipedia about-text" toggle. Verified:

- Initial `aria-checked="true"` (default ON).
- After click: `aria-checked="false"`, `localStorage` envelope
  contains `state.wikipediaIntegrationEnabled === false`.
- Second click: back to `aria-checked="true"`.
  Zero console errors.

#### Sub-track H — CSP for Wikipedia thumbnails + API hosts (~1h) ✅ DONE 2026-05-06

**Pre-existing-CSP correction (Codex round-5b audit, 2026-05-06)**:
audit confirmed atlas has **no Content-Security-Policy** in
`index.html` or `vite.config.ts` today. So this sub-track is
NOT just an allow-list addition — it's the wave that introduces
CSP at all. That changes the scope:

- Decide CSP delivery mechanism: HTML `<meta http-equiv>` (no
  server-config required, works for static hosting) vs
  HTTP response headers (better, but requires a hosting layer
  that supports per-route headers — atlas's deploy target
  matters here). Default: meta-tag for the M6 minimum-diff;
  revisit when atlas grows a server runtime.
- Establish a baseline policy that doesn't break what already
  works:
  - `default-src 'self'`
  - `img-src 'self' data: blob: https://upload.wikimedia.org`
    (data: + blob: needed by R3F/three for textures + canvas
    snapshots; verify against the existing image pipeline
    before committing)
  - `connect-src 'self' https://*.wikipedia.org`
    (NOT just `upload.wikimedia.org` — the API endpoints live
    on `en.wikipedia.org` / `pt.wikipedia.org` / etc., and
    fetch counts as `connect-src`)
  - `script-src 'self'` (Vite dev mode injects HMR scripts;
    may need `'unsafe-eval'` in dev OR conditionally inject
    only in prod build)
  - `style-src 'self' 'unsafe-inline'` (R3F + many React
    component libs use inline styles)
  - `font-src 'self' data:` (if any embedded fonts)
- Verify the policy doesn't regress:
  - Boot path (loader, R3F canvas init)
  - Texture load path (planet surfaces, sprites)
  - HMR in dev mode
  - Pixel-diff e2e specs (boot.spec.ts, postprocessing.spec.ts)
- Tests: build artifact has expected CSP meta; Playwright
  smoke verifies no CSP violations in the Console report
  during a normal session including a Wikipedia fetch.

**Shipped**:

- New `cspMetaTagPlugin` in `vite.config.ts` — injects a
  mode-aware `<meta http-equiv="Content-Security-Policy">`
  via Vite's `transformIndexHtml` hook (order: pre, before
  `react()`'s injections). Dev mode adds `'unsafe-eval'` to
  script-src + `ws:`/`wss:` to connect-src so HMR keeps
  working; prod tightens script-src to `'self'` only.
- Baseline directives (matching the wave-file spec):
  - `default-src 'self'`
  - `script-src 'self'` (prod) / `'self' 'unsafe-eval'` (dev)
  - `style-src 'self' 'unsafe-inline' https://fonts.googleapis.com`
    - `style-src-elem` mirror so the @import resolution
      (Tailwind compiles into inline styles + R3F/Drei set
      inline style attributes — `'unsafe-inline'` is unavoidable
      here without a hash-injection pipeline)
  - `font-src 'self' data: https://fonts.gstatic.com` (Google
    Fonts woff2)
  - `img-src 'self' data: blob: https://upload.wikimedia.org`
    (favicon SVG + base64 textures + R3F canvas snapshots +
    M6-D thumbnails)
  - `connect-src 'self' https://*.wikipedia.org` (M6-E REST
    API; dev adds `ws:`/`wss:` for HMR)
  - `worker-src 'self' blob:` (R3F's worker-loader)
  - `object-src 'none'` + `base-uri 'self'` + `form-action
'none'` — defense-in-depth tightenings beyond the spec
    minimum.
- Delivery via meta-tag (NOT HTTP headers) per the spec
  default — atlas ships as static-hosting bundle. Plugin
  comment notes the migration path for when atlas grows a
  server runtime.

**Boot smoke (Preview MCP, dev mode)**: fresh boot — zero
console errors. CSP meta-tag rendered, Rajdhani font loaded
from `fonts.gstatic.com`, R3F canvas mounted. Search +
focus on a curated body (Mars) → no errors. Search + focus
on a HYG star (Sirius) → Wikipedia fetch + thumbnail
(`upload.wikimedia.org/.../330px-Sirius_A_and_B_Hubble_photo.jpg`)
both render through the CSP. Pre-existing telemetry
"orbital engine failed" warnings on HYG focus IDs flagged
as a follow-up (Sidebar tries to call `useOrbitalCalculation`
with `hyg:K` IDs the engine doesn't know about — predates
M6-H, not CSP-related).

**Prod build verification**: `npm run build` emits
`dist/index.html` with the strict prod CSP (no `unsafe-eval`,
no ws/wss in connect-src). Diff confirmed via `grep
"Content-Security-Policy" dist/index.html`.

### Acceptance (M6)

1. Click a HYG star → HygStarPanel populates with name, spect,
   physical fields within ~50ms (from local catalog).
2. Wikipedia section loads within ~2s (network-dependent), shows
   extract + thumbnail; or shows "No Wikipedia article" if no
   match.
3. Click another HYG star mid-Wikipedia-fetch → first request
   aborts, second request runs fresh (no stale data flash).
4. Search "Sirius" → top suggestion is the HYG star, click →
   focus + fly-to + panel populates.
5. Search "α CMa" / "HD 48915" / "Gl 244" → all resolve to
   Sirius.
6. Search box still finds curated bodies ("Earth", "Mars") with
   priority over HYG matches when both could match.
7. Toggle `wikipediaIntegrationEnabled` off → HygStarPanel
   shows local fields only, no Wikipedia section, no fetch.
8. i18n: switch language to pt-BR → panel labels translate;
   Wikipedia client requests pt summary first, falls back to
   en if pt page doesn't exist.
9. Refresh page → cached summaries load from IndexedDB without
   network requests (verify via Network tab).
10. Production build CSP doesn't block Wikipedia thumbnail
    rendering.

### Dependencies + risk

- **i18n migration** of existing UI strings is OUT OF SCOPE for
  M6 — only NEW strings added by M6 use the i18n hook. Existing
  hardcoded English remains; future polish wave migrates them.
- **Wikipedia rate limit risk**: per ToS, sustained >1 req/s
  triggers temporary IP block. Atlas's queue serialization
  prevents this.
- **CSP regression risk**: adding `upload.wikimedia.org` to
  img-src is a controlled additive change; doesn't relax other
  origins.
- **Privacy**: with integration enabled (default), every HYG
  focus generates a Wikipedia REST request including the star
  name. Users opt out via the settings toggle.

---

## Round-5 (2026-05-06) — logisticSigmoid factor 12→60

**Status**: shipped at commit `079eb52`. **Awaiting user
re-smoke** to confirm the factor change resolves the U-1 jump
perception from 2026-05-05 smoke.

Wave-file S2 documented factor=12 as a deliberate "Atlas-Gaia
divergence" with the rationale that Gaia's 60 "feels stalled"
on 4-6 s transitions. User smoke disconfirmed: factor=12 puts
~50% of the trajectory progress in the first 35% of the
duration, which for a solar-system → Sirius fly-to (camera
distance changes ~3 orders of magnitude) means the camera
covers the bulk of the perceived scale within the first ~1.5 s
— solar system disappears in a flash, no perceptual departure
phase.

Round-5 reverts the divergence per `feedback_default_gaia_fidelity.md`:
factor=60 matches Gaia's `CameraModule.java:1210` clamp range
floor of 12 and Settings-fed default of 60. At factor=60:

- t∈[0, 0.3]: ~0% → ~0.5% trajectory progress (clear stall —
  "lift-off" phase, solar system stays in view)
- t∈[0.3, 0.7]: ~0.5% → ~99.5% trajectory progress (warp
  through scales)
- t∈[0.7, 1.0]: ~99.5% → 100% trajectory progress (clear stall
  — "arrival" phase, destination star fills view)

L40 lesson recurrence: the factor=12 rationale was an inferred
"feels stalled" judgment without empirical data. Round-5 is the
recurrence-correction (verified-against-source needs name +
semantics check, including "is this comparison-to-Gaia opinion
empirically grounded?").

---

## Round-6 — Interactive HYG flight (SHIPPED 2026-05-06)

**Status**: ✅ SHIPPED. All 8 sub-tracks landed across 7 commits
on 2026-05-06, plus a post-R6-H aim-lerp rewrite (3 more commits)
after user smoke surfaced "marcha ré" + "tela muda" perceptions:

- 89816e5 — R6-A+B+C kickoff (HygPhysicsFlight + OrientationLerp
  - setupCameraHyg rewire + useFrame physics dispatch).
- 9a8688e — Codex audit correction round 1 (added missing
  backward-flight branch + tightened no-warp unit test +
  skipped stale e2e).
- cbb767f — R6-E telemetry + R6-F first-guess calibration +
  log-stride criterion correction (plan-vs-source divergence
  fix: 3× linear-stride was structurally incompatible with the
  exp-decay shape Gaia produces; log-stride 1.5× is
  perception-correct per Weber-Fechner).
- 5336b70 — R6-G e2e respec + integrator sub-stepping
  (`MAX_DT_SUBSTEP=0.05 s`, defensive against R3F dt spikes
  from boot/suspend/headless-test rAF throttling).
- be2406f — R6-H close-out (telemetry scaffolding removed per
  L37; wave file / STATUS / ROADMAP / lessons.md synced).
- e5ea617 — Codex audit correction round 2 (3 doc fixes:
  hygFlightPosProgress.ts comments to journey-fraction
  semantic, DECEL_ONSET math to shipped constants, Sirius
  arrival test bracket tightened to 4-6 s; plus initial
  MAX_DT_TOTAL mitigation 5.0 → 1.0).
- c9a0c95 — Codex audit correction round 3 (P1 #1 real-fix —
  the prior 1.0 s cap actually rendered ~95 % of trajectory
  per frame on tab-resume because exp decay compounds; reduced
  to 0.1 s for ~26 % per-frame cap; e2e wait bumped 9.5 s →
  55 s + setTimeout 120 s → 140 s to absorb the slower
  integrator pace under 1 Hz headless R3F).

**Post-R6-H aim-lerp rewrite** (after first user smoke
surfaced "marcha ré" + "tela muda para um quadro errado"):

- 48e294c — band-aid retune of `oriDurationMs` from 1000-4000 ms
  to 200-500 ms (matched the round-5b-era duration to Round-6's
  fast position channel). **Superseded by 21d7bf3** — Codex
  audit demonstrated the band-aid was symptom-fix not
  root-cause, because the absolute-target lerp had structural
  failure modes that no duration would solve.
- 21d7bf3 — `AimLerp` rewrite. Replaces `OrientationLerp`
  (absolute target lerp from old focus → new star) with
  `AimLerp` (slerps the aim DIRECTION, derives target as
  `cameraPos + aimDir × distanceToStar` so target is always at
  the camera-to-star distance ahead of the camera in the slerp'd
  direction — never crosses, never degenerates). Adds
  `camera.lookAt(controls.target)` after writes in useFrame to
  force in-frame orientation update (Drei OrbitControls runs at
  `useFrame` priority -1, BEFORE the controller's priority 0,
  so the rendered quaternion would otherwise lag position by
  one frame). New `AimLerp` class + 16 unit tests covering
  geometric invariants (no crossing, slerp at 90°/180°,
  per-frame endpoint recompute), cancel semantics, degenerate
  inputs.
- 68e7fb7 — Codex doc-cleanup audit: 3 stale "OrientationLerp"
  references in `CameraController.tsx` updated to `AimLerp`;
  aim-lerp header doc cancel-semantics block reflects actual
  implementation (deactivates only, no return); JSDoc default
  factor=12 → factor=60 (matches `CameraTransition.logisticSigmoid`'s
  current default after round-5).

**M2.5 close gate**: user smoke at 4 named stars × 2 zoom cycles
to verify no warp perception. U-1 status flips to "shipped,
awaiting smoke". Any user-reported regression triggers an R6-F
empirical refinement pass with the calibration constants pinned
in `hygPhysicsFlight.ts:INITIAL_FORCE_FACTOR` /
`MAX_VELOCITY_FACTOR` / `FRICTION_RATE` /
`DECEL_ONSET_ANGULAR_RADIUS_RATIO`.

**History summary** (was: "promoted 2026-05-06 after Codex
external-friend audit demonstrated mathematically that round-5's
factor 12→60 cannot resolve U-1 no matter the value picked"):
Round-5 + round-5b stay shipped as they correctly close
divergences in atlas's scripted-flight contract — but that
contract is the wrong contract for the HYG focus-click use case,
which is structurally Gaia's `InteractiveCameraModule.go_to_object`
flow (per-frame velocity push driven by `NaturalCamera.java`'s
force/friction model). Round-6 ports that flow.

**Sub-track progress** (2026-05-06):

- **R6-A** ✅ shipped — `HygPhysicsFlight` core + `OrientationLerp`
  extraction (new `src/lib/camera/hygPhysicsFlight.ts`,
  `src/lib/camera/orientationLerp.ts`, barrel updated). Includes
  the Codex 2026-05-06 audit fix (P1 #2): two-direction integrator
  matching Gaia's `InteractiveCameraModule.go_to_object` forward /
  backward branch (`InteractiveCameraModule.java:172-200`).
- **R6-B** ✅ shipped — `setupCameraHyg` rewired to start the two
  channels in parallel; legacy `stellarFlightRef` removed from the
  controller (class retained in lib for future scripted-tour
  features per acceptance §5).
- **R6-C** ✅ shipped — useFrame branch dispatches physics +
  orientation independently; `setHygFlightPosProgress` semantics
  swapped from time-fraction to a direction-agnostic journey-
  fraction (post-Codex P1 #2 backward-flight support — gate-aligned,
  natural axis for M3 cross-fade in either direction).
- **R6-D** ⏳ pending — cancel-on-user-input handler is already
  wired via the existing OrbitControls "start" handler in
  `CameraController.tsx` to call both refs' `cancel()`; sub-track
  D is folded into the R6-B+C ship and only the focus-clear
  branch (line 168-176) remains, also already updated.
  Effectively done; close in R6-H along with the doc update.
- **R6-E** ✅ shipped + REMOVED at R6-H — telemetry ring buffer
  was wired behind `__ATLAS_DEBUG_HYG_PHYSICS__` window flag,
  used by R6-F first-guess calibration to confirm the integrator
  shape. Removed in the R6-H close commit per L37 (temp dev
  diagnostics must not ship beyond their wave). Production
  hardening that surfaced via the same telemetry pass —
  integrator sub-stepping (`MAX_DT_SUBSTEP` /
  `MAX_DT_TOTAL`) — STAYED, not telemetry.
- **R6-F** 🟡 first-guess shipped, empirical refinement pending —
  `MAX_VELOCITY_FACTOR=3.0` + `INITIAL_FORCE_FACTOR=8.0` chosen
  analytically: cap binds at `MAX_VELOCITY_FACTOR × distance/s`
  ⇒ Sirius arrival `ln(D/G)/3.0 ≈ 4.65 s` (in the 4-6 s
  Acceptance §1 band). Far stars arrive proportionally to
  `ln(distance)`, so Betelgeuse lands in ~1.7 s (vs wave-file
  7-10 s expectation). Single decay rate cannot reproduce the
  non-linear distance/time curve; user smoke should decide
  whether the linear-log decay shape feels right (matches
  Gaia's `speedScaling` shape, see test header) OR whether to
  introduce distance-dependent velocity scaling. Telemetry is
  in place for the empirical sweep.
- **R6-G** ✅ shipped — `e2e/hyg-focus.spec.ts` un-skipped.
  Round-5b's alpha-based timing assertions (mid-warp at t=2 s,
  landed at t=10 s) were replaced with end-to-end Round-6 contract
  assertions: pre-fly state + post-landing pose (mesh active,
  focusId survives, landing distance in `[400, 1000]` wu) +
  console clean + a single mid-fly orientation-lerp sample (uses
  `performance.now()` so it's R3F-frame-rate-independent). Mid-
  fly position-stride invariants moved to unit tests where
  Sirius-scale geometry can be exercised without R3F throttling.
  **R6-G discovery (2026-05-06)**: headless Chromium runs R3F at
  ~1 Hz, and the integrator's first frame after focus dispatch
  saw `dt ≈ 2.15 s` (catalog-load + boot accumulation). Semi-
  implicit Euler with that dt overshot Sirius by ~7×. **Fix**:
  `HygPhysicsFlight.update()` now sub-steps any `dt > MAX_DT_SUBSTEP
= 0.05 s` into multiple bounded integration steps, capping total
  per-call coverage at `MAX_DT_TOTAL = 5 s`. This keeps the
  integrator stable under any frame pacing (production normal-fps,
  production tab-suspend resume, test headless rAF throttling).
  Hardening surfaced via the e2e and unrelated to the R6 spec
  itself.
- **R6-H** ✅ shipped (this commit) — telemetry scaffolding
  removed (`telemetry` module-state, `isTelemetryEnabled`,
  `exposeTelemetry`, `recordTelemetry`, `tElapsed`,
  `__resetHygPhysicsTelemetry`, 3 unit tests covering the
  window-flag gate). `MAX_DT_SUBSTEP` / `MAX_DT_TOTAL` retained
  as production hardening. Wave file Round-6 status flipped to
  SHIPPED. STATUS.md fresh-loop fire flipped to user-smoke
  Round-6 acceptance. ROADMAP.md T6.4 effort estimate updated.
  lessons.md L41 added ("porting constants ≠ porting
  experience" — log-stride vs linear-stride criterion trap).

**Audit history** (Codex external-friend pass, 2026-05-06):

5 findings raised. 4 confirmed and corrected; 1 rejected after
verification:

- **P1 #1 (confirmed, fixed)** — Source-comment claim of "~5 s
  Sirius arrival" was wrong; corrected to reflect the actual
  ~46 s pending R6-F.
- **P1 #2 (confirmed, fixed)** — Missing backward-flight branch
  for the camera-rebound case (camera already inside gate
  distance). Two-direction integrator now ports Gaia's
  `go_to_object` semantics; +4 unit tests cover the backward
  flow.
- **P2 #3 (REJECTED after verification)** — Codex flagged the
  focus-clear path as "weaker than refinement #3" because
  `controls.target` isn't synced to the cancelled lerp's frozen
  value. Rejected: refinement #3 specifically targets the
  OrbitControls "start" handover (where OrbitControls is about
  to read the target); the focus-clear path has no consumer
  reading `controls.target` post-cancel (useFrame early-returns
  on `!focusId`). The previous useFrame already wrote the lerped
  target to controls; the discarded cancel value differs by
  ~16ms of lerp progress (indistinguishable). No regression. Codex
  confidence on this finding was 0.68, lowest of the five.
- **P2 #4 (confirmed, mitigated)** — `e2e/hyg-focus.spec.ts`
  asserted round-5 timing pins that no longer hold. Skipped via
  `test.skip` until R6-F + R6-G land; un-skip trigger documented
  in the test comment.
- **P3 #5 (confirmed, fixed twice)** — No-warp unit test was
  looser than the wave-file plan (10× window-style spread vs
  plan's 3× at t=1..2..3s). The audit-fix commit (9a8688e)
  applied the plan's 3× criterion verbatim. **Subsequent R6-E/F
  preliminary work (2026-05-06)** discovered the 3× criterion
  is structurally incompatible with exp decay, which is
  Gaia-faithful per `NaturalCamera.java:1421-1438`'s
  distance-proportional `speedScaling`. Criterion re-revised
  to log-stride 1.5× (perception-correct, Gaia-source-aligned)
  — see Acceptance §1 inline note above for the rationale.

**Trigger** (no longer "if round-5 fails"): independent of any
user re-smoke, the analysis below shows round-5's pre-computed
lerp + sigmoid model concentrates the entire perceived motion
of a solar→star fly-to into a ~825 ms warp window covering
99.5 % of trajectory progress (sub-frame visual transit through
the solar system at ~6.4e8 wu/s). That is structurally
incompatible with a "fly out of the solar system, cross
interstellar space, arrive at a star" perception.

### Architectural diagnosis

Atlas's M2.5 (S1-S7 + round-3..5b) ports Gaia's
**`CameraModule.transition_position`** flow:
linear `LinearDouble` path interpolation (`CameraModule.java:1229-1232`)

- `logisticsigmoid` mapper (`CameraModule.java:1207-1218`).
  That is Gaia's **scripted/cinematic** transition — used by tour
  playback and explicit `transition()` API calls, where the
  caller is treating the camera as a movable prop along a
  pre-determined path with predictable timing.

Gaia has a **second, structurally different** flow for the
"user clicked a star" case:
**`InteractiveCameraModule.go_to_object`**
(`InteractiveCameraModule.java:174-200`). This loop posts
`Event.CAMERA_FWD` events frame by frame with `dt` in ms until
`focusView.getSolidAngle() ≥ target`. The "feel" comes from
**`NaturalCamera`**'s force/velocity/friction model
(`NaturalCamera.java:125,341,985-1011,1533-1537`): camera
accelerates from rest, friction kicks in after ~500 ms of
input stillness, terminal velocity is bounded by friction +
distance, deceleration is automatic as angular size grows
toward the target gate.

The user-perceived "jump" is the difference between these two
flows. Atlas's HYG focus-click was wired to the scripted flow,
which gives **predictable timing at the cost of perceptual
naturalness across scales**. Round-6 wires it to the
interactive flow, which gives **perceptual naturalness at the
cost of predictable timing** (camera arrives when the angular
gate is satisfied, not at a fixed alpha=1).

Round-5's factor 12→60 + round-5b's oriEasing factor=17 are
**correct for a future scripted-tour feature** (e.g. a "guided
tour" mode where the timing and pacing are deliberately
choreographed). They are wrong for click-driven focus. Both
flows live in the codebase post-Round-6, with separate call
sites:

- HYG focus-click → new `HygPhysicsFlight` (Round-6).
- Curated solar-system fly-to → existing `CameraTransition`
  (untouched).
- Future scripted-tour → existing `StellarFlightTransition`
  (kept, documented as scripted-only).

### Source citations (verified 2026-05-06)

- **`InteractiveCameraModule.java:174-200`** — scripted
  while-loop firing `Event.CAMERA_FWD` per frame with `dt` until
  `focusView.getSolidAngle() ≥ target`. The control-loop layer.
- **`NaturalCamera.java:125`** — `private Vector3Q friction;`
  (member declaration).
- **`NaturalCamera.java:341`** — `friction = new Vector3Q();`
  (allocation).
- **`NaturalCamera.java:985-1011`** — friction integration:
  after 0.5 s of no zoom input, "real friction kicks in";
  `friction.set(vel).scl(-...)` decelerates against velocity;
  `force.add(friction)` blends. The shape Round-6 ports.
- **`NaturalCamera.java:1011`** — `force.add(friction)`.
- **`NaturalCamera.java:1533-1537`** — `case CAMERA_FWD -> {
addForwardForce((double) data[0]) }` translates the scripted
  `dt` payload into a velocity contribution. The handshake
  between the two files.

### Refinements from Codex audit (2026-05-06, all accepted)

The first Round-6 draft (in commit `caf0c11` round-5b) carried
five subtle errors that would have produced bad UX even after
the architectural promotion. All five verified against atlas
code or first-principles math before accepting:

1. **Smooth orientation, not snap.** Earlier draft proposed
   `controls.target.copy(starPos)` at click — that is exactly
   the snap M2.5 fixed (pre-M2.5 spin-then-glide bug). Keep an
   orientation channel for 300-500 ms (lerp `controls.target`
   from `startTarget` to `starPos` with `logisticSigmoid
factor=17` per round-5b). The position physics runs in
   parallel with the orientation lerp; both are independent
   channels with independent durations as in M2.5 S3.

2. **Calibrate, don't blind-port.** Gaia's `NaturalCamera`
   constants (force, friction rate, terminal velocity) are
   tuned for libGDX desktop (different framerate),
   parsec-internal coordinates (Atlas uses 1 wu = 0.001 AU),
   and Gaia's own Settings cinematic flags. Round-6 ports the
   **shape** (initial accel from rest → max velocity bounded
   by remaining distance → friction-based decel near gate) and
   calibrates numbers via dev-mode telemetry sweep (sub-track
   R6-F).

3. **Cancel = full stop, not drain.** Earlier draft said
   "cancel = stop force, let friction settle the camera". That
   works in Gaia because the same physical camera also reads
   user input. Atlas has `OrbitControls` as a separate input
   system; if the user drags mid-flight, the autopilot cannot
   keep coasting on inertia (would fight the user's drag).
   Round-6 cancel: `velocity = 0, force = 0` synchronously,
   sync `controls.target` to current state, hand over to
   OrbitControls. Same handover pattern as M2.5 S5
   (`CameraController.tsx:588-623`).

4. **`targetAngularRadiusRad`, not `targetSolidAngle`.** L40
   recurrence trap: round-3 C-1 explicitly renamed everything
   to angular radius (`stellarFlightSolidAngle.ts:166,195`)
   because Gaia's `getSolidAngle()` returns
   `(radius/distance)/fovFactor` — angular-radius semantics
   despite the misnamed method. Round-6 must consistently
   use `angularRadiusRad` in its public API and internals.
   "solidAngle" reserved for citations of Gaia source.

5. **Semi-implicit Euler, not analytical kinematic.** With
   variable acceleration (friction depends on current
   velocity, which changes per frame), the analytical formula
   `pos += v·dt + 0.5·a·dt²` is only exact for `a` constant
   over `dt`. Semi-implicit Euler (`v += a·dt; pos += v·dt`)
   is symplectic, energy-stable in oscillatory regimes, uses
   the updated velocity to advance position (more responsive
   to acceleration changes), and is the canonical integrator
   for game physics with friction. Atlas adopts this.

### Spec — 8 sub-tracks, ~10-14 h total

#### R6-A — `HygPhysicsFlight` core model (~3 h)

New `src/lib/camera/hygPhysicsFlight.ts`. Pure-function /
class library, no React. State: `position` (THREE.Vector3),
`velocity` (Vector3), `currentAngularRadiusRad` (number,
derived per-frame from `radiusWu / distanceToTarget`),
`targetAngularRadiusRad` (number, fixed at start),
`targetPos` (Vector3), `radiusWu` (number).

Per-frame `update(dt)` (semi-implicit Euler):

```ts
// 1. Compute force toward target (initial accel, ramp up from rest).
const toTarget = targetPos.clone().sub(position);
const distanceToTarget = toTarget.length();
const forceDir = toTarget.normalize();

// 2. Friction proportional to velocity (decel near gate).
const friction = velocity.clone().multiplyScalar(-FRICTION_RATE);

// 3. Force magnitude — ramp up from rest, zero out once we cross
//    the decel-onset gate so the camera coasts to a stop on
//    friction alone.
const angularRatio = currentAngularRadiusRad / targetAngularRadiusRad;
const inDecelPhase = angularRatio > DECEL_ONSET_ANGULAR_RADIUS_RATIO;
const forceMagnitude = inDecelPhase
  ? 0
  : INITIAL_FORCE_FACTOR * distanceToTarget;

const acceleration = forceDir.multiplyScalar(forceMagnitude).add(friction);

// 4. Semi-implicit Euler integration.
velocity.add(acceleration.multiplyScalar(dt));
// 5. Cap velocity by remaining-distance heuristic.
const maxVelocity = MAX_VELOCITY_FACTOR * distanceToTarget;
if (velocity.length() > maxVelocity) {
  velocity.setLength(maxVelocity);
}
position.add(velocity.clone().multiplyScalar(dt));

// 6. Recompute angular radius for gate check.
currentAngularRadiusRad = radiusWu / position.distanceTo(targetPos);
const done = currentAngularRadiusRad >= targetAngularRadiusRad;
```

API:

```ts
interface HygPhysicsFlightSpec {
  startPos: Vector3;
  targetPos: Vector3;
  targetAngularRadiusRad: number;
  radiusWu: number;
  onComplete?: () => void;
}

class HygPhysicsFlight {
  start(spec: HygPhysicsFlightSpec): void;
  /** Returns null when inactive. Otherwise advances by dt and
   *  returns the new position + done flag. */
  update(dt: number): { position: Vector3; done: boolean } | null;
  /** Full stop: velocity=0, force=0, returns the frozen position
   *  for caller to sync OrbitControls. Inactive after. */
  cancel(): { position: Vector3 } | null;
  get isActive(): boolean;
}
```

#### R6-B — Integrate in `setupCameraHyg` (~2 h)

`src/components/canvas/CameraController.tsx setupCameraHyg`
substitutes the `stellarFlightRef.current.start({...})` call's
**position channel** with `hygPhysicsRef.current.start({...})`.

Crucially, the **orientation channel of M2.5** is preserved:
`stellarFlightRef.current.start({...})` is still called, but
only with `startTarget`/`endTarget` driving the lerp; the
position arms/endPos are no longer relevant. Or alternatively
extract a smaller `OrientationLerp` class to avoid threading
orientation-only state through `StellarFlightTransition`.
Decision: extract `OrientationLerp` for clarity (sub-track
R6-A includes the small extraction).

`computeAtlasFlightLanding` still computes
`{ distanceWu, target.angularRadiusRad }` from M2.5 — the
**`angularRadiusRad`** is what Round-6 uses as the gate
target (not the `distanceWu`, which is now derived
post-physics-stop).

#### R6-C — useFrame branch dispatching position+orientation (~2 h)

`useFrame` reads:

- `hygPhysicsRef.current.update(dt)` for position
- `orientationLerpRef.current.update()` for `controls.target`

Both run in parallel until both complete. Position done →
camera holds position. Orientation done → `controls.target`
holds star position. Mutual exclusion with curated
`transitionRef` stays via the same isFlying flag.

#### R6-D — Cancel on user input (~1 h)

`OrbitControls "start"` handler (existing, line 588) extends:

- `hygPhysicsRef.current.cancel()` → returns `{ position }`,
  apply nothing (camera position is already at frozen state
  since useFrame wrote per-frame).
- `orientationLerpRef.current.cancel()` → returns
  `{ target }`, `controls.target.copy(frozen.target)` (same
  pattern as M2.5 S5).
- `flyingRef.current.isFlying = false`.
- `setHygFlightPosProgress(null)`.

Round-4 C-5 (focus-clear cancel) extends similarly: cancel
both refs in the focusId-becomes-null branch.

#### R6-E — Calibration telemetry (debug-only, ~1 h)

Behind `__ATLAS_DEBUG_HYG_PHYSICS__` flag (matching the
existing `__ATLAS_TEST_FREEZE__` gate pattern). When set,
`HygPhysicsFlight.update` writes per-frame telemetry to a ring
buffer accessible via `window.__ATLAS_HYG_PHYSICS_TELEMETRY__`.
Fields: `t`, `velocityMagnitude`, `currentAngularRadiusRad`,
`distanceToTarget`, `forceMagnitude`, `frictionMagnitude`,
`done`. Used by R6-F for empirical tuning. **Removed in the
final commit before R6 close** (per L37 — temp dev diagnostics
must not ship).

#### R6-F — Empirical calibration sweep (~2 h)

Run `npm run dev` with `__ATLAS_DEBUG_HYG_PHYSICS__=true`. Test
at named anchors:

- Sirius (2.6 pc, ~5.36e8 wu): expect 4-6 s feel, no warp
  perception, smooth deceleration last second.
- Betelgeuse (197.8 pc, ~4.08e10 wu): expect 7-10 s, longer
  acceleration phase, terminal velocity reached.
- Far-edge anchor (2805 pc, ~5.78e11 wu): expect 10-15 s,
  bounded by terminal velocity.
- Camera-rebound (already near Sirius, click Sirius again):
  expect quick 1-2 s settle.

Tune four constants:

```ts
INITIAL_FORCE_FACTOR; // wu/s² per wu of remaining distance
MAX_VELOCITY_FACTOR; // dimensionless, vmax = factor·distance/s
FRICTION_RATE; // 1/s (exponential decay)
DECEL_ONSET_ANGULAR_RADIUS_RATIO; // dimensionless
```

Starting points (subject to R6-F revision):

```ts
INITIAL_FORCE_FACTOR = 0.5; // gentle ramp
MAX_VELOCITY_FACTOR = 0.3; // 30% of remaining-distance per second
FRICTION_RATE = 2.0; // 2 friction-time-constants per second
DECEL_ONSET_ANGULAR_RADIUS_RATIO = 3.0;
```

With `MAX_VELOCITY_FACTOR=0.3` for Sirius (5.36e8 wu): max
vel = 1.6e8 wu/s, traversal would be ~3.3 s without ramp; with
1 s acceleration + 1 s decel, real ~5 s. Solar system stays in
view for ~1-2 s before camera hits perceptible velocity. This
is the "log-feel without log-lerp" Codex (external-friend
audit) flagged as the architectural goal.

Pin the calibrated constants in `hygPhysicsFlight.ts` with
named-anchor expected-time comments.

#### R6-G — Tests (~3 h)

- vitest pure-function tests for the physics state machine:
  start, accelerate-from-rest, terminal velocity, decel onset
  at angular ratio, gate stop, cancel-zero behavior. Use
  `vi.useFakeTimers()` to drive `update(dt)` deterministically.
- Update `e2e/hyg-focus.spec.ts`. Round-5b's spec asserted
  alpha-based timings (target lerp at t=0.5s, mid-warp at
  t=2s, landed at t=10s). Round-6 model has no explicit
  alpha — replace with **angular-radius progression**
  assertions: at sample times, `currentAngularRadiusRad`
  must monotonically grow (no warp/snap), and at landed
  sample must be `≥ targetAngularRadiusRad`. Landing-
  distance bracket assertion stays (`[400, 1000] wu` for
  Sirius), since the final pose is still gate-driven.
- Add a Round-6 specific test: focus a HYG star, sample
  position at three midpoints, assert the position changed
  by a SIMILAR magnitude between samples (no single-frame
  warp). **Criterion correction (2026-05-06 R6-E/F prelim)**:
  the original "|Δpos| at t=1..2..3 s within 3×" criterion
  is structurally incompatible with the exp-decay shape
  Gaia produces — `NaturalCamera.java:1421-1438`'s
  `speedScaling` is a `flint`-piecewise-linear interpolation
  of focus distance, so per-frame force is roughly distance-
  proportional, giving exponential decay (consecutive 1 s
  strides differ by `exp(k)`). Human perception is
  logarithmic (Weber-Fechner) — constant log-stride means
  apparent size grows at a constant rate in log-perceptual
  space, which IS subjectively smooth. Updated criterion:
  `log(d_n / d_{n+1})` consistent within 1.5× across
  t=1..2..3 s (tolerance accounts for ramp-up bleed into the
  first window + integrator jitter). The cruise-window
  monotonicity + step-cap shape assertions still apply.

#### R6-H — Wave + STATUS + ROADMAP + lessons.md update (~1 h)

- Wave file: this section flips from spec to "shipped at
  commit X"; M2.5 wave acceptance §3 (durations) updated to
  reflect physics not lerp.
- STATUS.md: Default fresh-loop fire flips to "user smoke
  Round-6 acceptance: 4 stars × 2 cycles, verify no warp
  perception"; U-1 status flips to "shipped, awaiting smoke".
- ROADMAP.md: T6.4 effort estimate updated.
- lessons.md L41 (new): "Porting constants ≠ porting
  experience" — see lessons.md.

### Acceptance (Round-6)

1. **No warp perception**: solar→Sirius fly-to has visible
   "leaving the solar system" phase (1-2 s where solar bodies
   shrink in view), continuous middle phase (camera covers
   interstellar distance with apparent motion, no
   single-frame jumps), continuous "approaching star" phase
   (Sirius grows from a point to its disc).
2. **Landing pose matches M2.5 contract**: Sirius lands at
   `~456 wu` from camera (post-C-1 angular-radius math),
   Betelgeuse at `~254 000 wu`, etc. Numeric pins from M2.5
   tests still hold.
3. **Mid-flight interrupt**: user drag during physics flight
   stops the autopilot immediately (`velocity=0, force=0`,
   not "drain friction"). No fight between OrbitControls and
   physics.
4. **Curated-body fly-to byte-identical**: existing
   `CameraTransition` path for solar-system bodies untouched.
5. **`StellarFlightTransition` still passes its tests**:
   reserved for future scripted/cinematic features. No
   active call site uses it for click-driven focus after
   Round-6.
6. **Calibration constants pinned with rationale**: each of
   the 4 constants has a comment explaining its named-anchor
   expected-time and the reasoning for the chosen value.
7. **Telemetry removed**: `__ATLAS_DEBUG_HYG_PHYSICS__` flag
   - the `window.__ATLAS_HYG_PHYSICS_TELEMETRY__` exposure
     are gone from the ship commit (search-clean for the
     string).
8. **User smoke**: 4 named stars × 2 zoom cycles, all PASS
   the no-warp criterion.

### Dependencies + risks

- **Calibration is the single largest risk**. The 4
  constants compose multiplicatively; bad combinations could
  produce overshoot (camera blows past gate), under-shoot
  (camera never reaches gate within reasonable time),
  oscillation (under-damped), or feel-stalled (over-damped).
  R6-F (calibration sweep) is the de-risk step — test before
  pinning.
- **OrbitControls fight**: physics writes to `camera.position`
  per frame; OrbitControls also reads camera state per frame.
  Verify no race condition (e.g., ensure physics runs BEFORE
  OrbitControls.update() in the useFrame priority order).
  Existing M2.5 useFrame ordering should already be correct —
  re-verify.
- **Out-of-scope**: no quaternion slerp on (dir, up) like
  Gaia. Atlas keeps OrbitControls-derived orientation. The
  M2.5 §S4 option-2 strategy still applies.
- **Out-of-scope**: no automatic re-centering / orbital
  insertion at landing. Camera arrives at landing pose and
  hands over to OrbitControls + focus-tracking (M2.5 default).

---

## M7 — Final cleanup + smoke

**Effort**: ~30min.
**Status**: pending.
**Depends on**: M1-M5 PASS (M6 either landed or deferred).

- Remove any remaining dev diagnostics if introduced during
  M1-M6 debug
- Final preview-MCP boot smoke (`gl.isContextLost()===false`,
  level:error console clean)
- L26 multi-frame readPixels invariant (≥30 rAF, static regions
  zero RGB variance)
- User does a final cycle smoke (4 named stars × 2 zoom cycles +
  quality-flip while focused on HYG)

---

## Wave acceptance

T6.4 visual-recovery **core** ships ONLY when M1-M5 + M7 land

- user explicitly confirms visual delivery matches expectation
  (fly-to + procedural mesh visible + smooth transition + class
  distinction + supergiants properly sized).

**M6 was promoted from OPTIONAL polish to active forward-port
2026-05-05** after user smoke surfaced search-box and
info-panel gaps as real UX requirements (U-2, U-5 in STATUS
Carryover). See §M6 above for the 8-sub-track spec
(i18n + HYG binary v3 + search + HygStarPanel + Wikipedia REST

- IndexedDB cache + settings toggle + CSP). M6 is independent
  of the M1-M5+M7 core, can land in parallel post-M2.5, but does
  NOT block the core ship. No "MVP closed" claim until
  user-driven smoke passes the core (M1-M5 + M7); M6 acceptance
  sits separately at its own §M6 acceptance block.

---

## Dependencies + blockers

- **Depends on (all shipped)**: T6.0 (`e52561f`) + T6.1
  (`003002c`) + T6.2-α (`b1f7dc1`) + T6.2-β-α (`8c1f37d`) +
  T6.2-β-β (`2bfb970`) + T6.3-α (`9be5bfe`) + T6.3-β
  (`f16ca78`) + T6.3-γ (`f02aec8`) + T6.3-δ (`4d38251`) +
  T6.3-ε (`00e9a5a`).
- **Blocks**: T-Closeout (asset-licensing audit), T4.3 (particle
  pipeline), T5.2/T5.6 PNG re-bakes — all deferred until T6.4
  core ships.

---

## Audit history (all addressed before this wave file landed)

- **Round 1** (2026-05-04, post-T6.3-γ ship): caught P1
  frame-loop early-return + P2 small-star clamp + P2 skipMask
  tier-flip. Fixed in T6.3-δ (`4d38251`).
- **Round 2** (2026-05-04, post-T6.3-δ): caught P2 quality
  strand + P3 click-bypass selection + P3 projection lines stale
  - P3 spect coverage doc. Fixed in T6.3-ε (`00e9a5a`).
- **Round 3** (2026-05-05, on T6.4 plan commit `68b1d9f`): caught
  P1 STATUS pipeline stale + P2 false Float32Array claim + P2
  M2 contract gap + P2 M3 global LEN0 risk + P2 M5 false premise
  - P3 M6 stellarPhysicsFrom non-existence. Fixed in `1f20e36`.
- **Final pass** (2026-05-05, on `0b4c648`): caught P2 ROADMAP
  M6 still mandatory + P2 STATUS T4.1 float32-comfortable
  refuted + P3 deprecated `npm test --run`. Fixed in `3b011d5`.

---

## See also

- `tasks/archive/postmortems/T6-visual-failure.md` — full
  narrative of the 2026-05-04 user smoke + 4 root causes +
  precision-bomb numerical analysis.
- `tasks/STATUS.md` (hot path) — Active wave pointer + Carryover.
- `tasks/lessons.md` — L37 (rendering claims need runtime smoke)
  - L38 (cross-doc consistency as separate verification step) +
    Gaia-pattern adapt rule.
- `tasks/ROADMAP.md` — strategic index (this wave file is the
  canonical detail).
