- **VERDICT:** **No (cannot be 1:1 in the code I can verify).** In `fgferre/atlas-orbital` `main`, the post-processing chain contains **Bloom → ToneMapping → HueSaturation → BrightnessContrast** and **does not include any lens-flare / lens-dirt / light-glow passes**, so it cannot match Gaia Sky’s runtime lens-flare pipeline. `src/components/canvas/scene/PostProcessingPipeline.tsx:105` `src/components/canvas/scene/PostProcessingPipeline.tsx:117` `src/components/canvas/scene/PostProcessingPipeline.tsx:120` vs `core/src/gaiasky/render/postprocess/effects/LensFlare.java:120` `core/src/gaiasky/render/postprocess/effects/LensFlare.java:140`

- **EVIDENCE:**
  - **Atlas effect order (no lens flare present):** `EffectComposer` mounts only `<Bloom/>`, `<ToneMapping/>`, `<HueSaturation/>`, `<BrightnessContrast/>`. `src/components/canvas/scene/PostProcessingPipeline.tsx:105` `src/components/canvas/scene/PostProcessingPipeline.tsx:110` `src/components/canvas/scene/PostProcessingPipeline.tsx:117` `src/components/canvas/scene/PostProcessingPipeline.tsx:119`
  - **Gaia lens flare is a dedicated effect with ping-pong + optional dirt + combine:** `LensFlare.render()` writes flare into a ping-pong buffer, optionally runs `LensDirtFilter`, then combines with the original scene. `core/src/gaiasky/render/postprocess/effects/LensFlare.java:118` `core/src/gaiasky/render/postprocess/effects/LensFlare.java:122` `core/src/gaiasky/render/postprocess/effects/LensFlare.java:124` `core/src/gaiasky/render/postprocess/effects/LensFlare.java:140`
  - **Gaia forces a float-format internal buffer for lens flare:** `Pixmap.Format.RGB888` is used “to force internal format GL_RGB16F”. `core/src/gaiasky/render/postprocess/effects/LensFlare.java:41` `core/src/gaiasky/render/postprocess/effects/LensFlare.java:46`
  - **Gaia “COMPLEX” flare is clamped and uses the 10-iteration accumulator + hex shape:** loop `for (int i = 0; i < 10; i++)` and `regShape(..., 6)`, then final clamp. `assets/shader/postprocess/lensflare.frag.glsl:147` `assets/shader/postprocess/lensflare.frag.glsl:128` `assets/shader/postprocess/lensflare.frag.glsl:203`
  - **Gaia flicker driver is more plausibly the luma-based occlusion sampling (not `sin(l*30.0)`):** intensity is `u_intensity * lum * light_intensity`, where `lum` is averaged from spiral samples of `u_texture0`. `assets/shader/postprocess/lensflare.frag.glsl:188` `assets/shader/postprocess/lensflare.frag.glsl:192` `assets/shader/postprocess/lensflare.frag.glsl:197` (the `sin(l*30.0)` term is only inside one ring component) `assets/shader/postprocess/lensflare.frag.glsl:127`

- **DIVERGENCES (verified from source):**
  - **High:** Atlas `main` postprocessing pipeline does not wire any Gaia-style lens-flare path at all. **Fix sketch:** add a dedicated lens-flare pass (and lens-dirt modulation) and combine additively with the scene, matching Gaia’s `flare → dirt? → combine` structure. `src/components/canvas/scene/PostProcessingPipeline.tsx:105` `core/src/gaiasky/render/postprocess/effects/LensFlare.java:120`
  - **High:** Gaia explicitly uses a float internal format for the lens-flare ping-pong (GL_RGB16F via `RGB888`), which directly targets banding/artifact issues. **Fix sketch:** ensure the atlas lens-flare intermediate render target is half-float/float (not RGBA8) before any clamp/add. `core/src/gaiasky/render/postprocess/effects/LensFlare.java:41` `core/src/gaiasky/render/postprocess/effects/LensFlare.java:46`
  - **Medium:** Gaia’s algebra is **not** “single-pass equivalent”: flare output is clamped (`lensflare.frag.glsl`) **before** dirt multiplication (`lensdirt.frag.glsl`) and **then** added to base (`combine.frag.glsl`). If atlas inlines these in a different clamp/order, it will not match. **Fix sketch:** replicate Gaia’s clamp points and pass boundaries. `assets/shader/postprocess/lensflare.frag.glsl:203` `assets/shader/postprocess/lensdirt.frag.glsl:34` `assets/shader/postprocess/combine.frag.glsl:55`

- **BLINDSIDES CHECKED (1–10):**
  1. **Partially verifiable:** Gaia’s end-to-end lens flare path is `LensFlare(render): flare → dirt? → combine`. Atlas equivalent wiring not found in `main`. `core/src/gaiasky/render/postprocess/effects/LensFlare.java:118` `src/components/canvas/scene/PostProcessingPipeline.tsx:105`
  2. **Non-issue in Gaia:** `nLights` defaults to `0`, so `(0,0)` light positions cannot “flash” unless explicitly set. `core/src/gaiasky/render/postprocess/filters/LensFlareFilter.java:16` `core/src/gaiasky/render/postprocess/filters/LensFlareFilter.java:80`
  3. **Real issue class in Gaia (and likely relevant to your symptom):** Gaia forces GL_RGB16F for the flare ping-pong to avoid artifacts. `core/src/gaiasky/render/postprocess/effects/LensFlare.java:41` `core/src/gaiasky/render/postprocess/effects/LensFlare.java:46`
  4. **Real difference (Gaia is multi-pass with multiple clamps):** `lensflare` clamps; `lensdirt` multiplies then clamps; `combine` adds then clamps. `assets/shader/postprocess/lensflare.frag.glsl:203` `assets/shader/postprocess/lensdirt.frag.glsl:34` `assets/shader/postprocess/combine.frag.glsl:55`
  5. **Gaia semantics confirmed:** starburst uses `mod(abs(radial - u_starburstOffset), 1.0)` and mirrors with `-radial`, then clamps. `assets/shader/postprocess/lensdirt.frag.glsl:29` `assets/shader/postprocess/lensdirt.frag.glsl:32`
  6. **Gaia input buffer confirmed:** occlusion/luma sampling reads `u_texture0` (the effect input). `core/src/gaiasky/render/postprocess/effects/LensFlare.java:116` `assets/shader/postprocess/lensflare.frag.glsl:192`
  7. **Couldn’t verify for atlas:** no `EffectAttribute.CONVOLUTION` usage found because the atlas lens-flare effect files listed in the prompt are not present in `main`. `src/components/canvas/scene/PostProcessingPipeline.tsx:105`
  8. **Gaia intensity chain confirmed:** per-light intensity scales `u_intensity * lum * light_intensity`. `assets/shader/postprocess/lensflare.frag.glsl:197` `core/src/gaiasky/render/postprocess/filters/LensFlareFilter.java:60`
  9. **Non-issue (Gaia COMPLEX ignores `u_color`):** `circle()` overwrites `color` with the cosine palette. `assets/shader/postprocess/lensflare.frag.glsl:130` `assets/shader/postprocess/lensflare.frag.glsl:131`
  10. **Couldn’t verify atlas-side bloom feedback loop:** atlas `main` has bloom + tonemap only; no lens flare stage to test interaction. `src/components/canvas/scene/PostProcessingPipeline.tsx:105`

If you want, I can redo the comparison against your _local working tree_ specifically (instead of GitHub `main`) if you paste the contents of the atlas files you listed (or at least `LensFlareEffect.ts`, `LightGlowEffect.ts`, `PseudoLensFlareEffect.ts`, and `Scene.tsx` renderer config blocks).
