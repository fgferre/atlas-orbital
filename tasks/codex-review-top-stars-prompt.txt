You are validating the θ.1b star-rendering pipeline in atlas-orbital
(a Three.js/R3F port of Gaia Sky's star renderer). The goal is 1:1
visual parity with Gaia Sky's `star.group.quad.vertex.glsl` +
`star.group.quad.fragment.glsl`.

## Context

We just shipped a correction (commits `f8d8bff` + `8668b20`) replacing
a Stefan-Boltzmann physical-radius port with Gaia Sky's
source-authoritative **pseudo-size** derived from absolute magnitude:

  pseudoL = 10^(-0.4 · absMag)
  size    = sqrt(pseudoL) × 0.15  (parsecs)

Pipeline (`src/components/canvas/Starfield.tsx` + `src/lib/starPhysics.ts`):
  1. HYG binary → apparent mag + distance → absolute mag
  2. absMag → pseudoSize (pc) via `absoluteMagnitudeToPseudoSize`
  3. a_size (scene units) = pseudoSize × DISTANCE_SCALE × STAR_SIZE_FACTOR
  4. shader: solidAngle = a_size / dist
  5. opacity = lint_smoothstep(solidAngle, u_solidAngleMap, u_opacityLimits)
  6. solidAngle = clamp(pow via degrees12/radians12 wrap, u_minQuadSolidAngle, 3e-8)
  7. gl_PointSize = solidAngle × u_sizeFactor × pixelsPerRadian
  8. fragment: alpha = opacity × u_alphaFactor × boundaryFade; color from bvToRGB × vfxHdrGain

User reports visually "Capella looks larger than Sirius". Our math
says otherwise — Sirius clamps at 3e-8 → 50.5 px, Capella at 31.7 px
(see `tasks/audit-top-stars-pixels.md` generated by
`scripts/audit-top-stars-pixels.mjs`).

## Your Audit Task

Answer ALL of the following, citing source file:line when making claims.

### Part 1: Math validation (the audit table)

The generated table pins the pipeline output for the top-15 brightest
HYG stars. Verify:

 1. **Pseudo-size formula correctness.** For each star, does
    `sizePc = sqrt(10^(-0.4 · absMag)) × 0.15` match the Java
    `AstroUtils.absoluteMagnitudeToPseudoSize` at
    `/tmp/gaiasky/core/src/gaiasky/util/coord/AstroUtils.java:463-475`?
    Pick Sirius, Betelgeuse, Capella, spot-check.

 2. **Distance modulus correctness.** `apparentToAbsMag(mag, dist) =
    mag - 5·log10(dist/10)` — matches Gaia's
    `apparentToAbsoluteMagnitude` at `AstroUtils.java:441-445`?

 3. **Invariance of final pixel size to distance within a given
    apparent magnitude.** The audit table shows ties: Arcturus (dist
    11.26 pc) = Rigil Kent (1.32 pc) = Vega (7.68 pc) all render at
    33.2 px because HYG quantizes mag to 0.1 and all three have mag=0.0.
    Is this the intended Gaia Sky behavior? (Hint: apparent magnitude
    IS by definition a distance-normalized brightness. If the port
    uses apparent mag through pseudo-size-from-absMag-via-distance-
    modulus, then brightness should be fully captured by apparent
    mag alone.) Prove or refute mathematically by substituting the
    distance modulus back into the pseudo-size formula.

 4. **Ordering by pixel size MUST track ordering by apparent magnitude.**
    Sirius (mag -1.40) > Canopus (mag -0.60) > stars-at-mag-0.00 > ...
    Is this the expected Gaia Sky behavior, or should intrinsic
    luminosity (absMag) play a role too?

### Part 2: Pipeline bugs that could invert the visual ordering

Independently of the math table, audit `src/components/canvas/Starfield.tsx`
for any shader / material / post-process effect that could make
Capella (mag 0.1, warm yellow/orange) APPEAR larger than Sirius
(mag -1.4, cool blue-white) on-screen despite Sirius having ~60%
more pixels. Specifically check:

 a. **GL_POINTS size cap.** `gl_PointSize` may be clamped by the
    driver. Is there a `GL_POINT_SIZE_MAX` constant set or queried?
    On most drivers 64-256 px is typical. If Sirius (50.5 px) is
    BELOW the cap but a larger star saturates at the cap, no issue —
    but worth flagging.

 b. **Bloom / selective-HDR interaction.** The fragment outputs
    premultiplied rgb = (vColor + core*2) * alpha. `vColor = bvToRGB(ci)
    × vfxHdrGain`. If `bvToRGB` returns vec3(1,1,1) for Capella and
    vec3(0.5, 0.7, 1) for Sirius (hypothetical), then Capella's LUMA
    could be higher despite the same sprite area — tripping the
    Bloom `luminanceThreshold = 1.0` harder. Check `bvToRGB` at
    `Starfield.tsx:127-138` and the Bloom integration. Does the
    3-branch piecewise output luma > 1 for any input, so Capella
    could bloom more than Sirius?

 c. **Fragment premultiplication + blending.** Commit history says
    blending is OneFactor/OneFactor additive; fragment premultiplies
    by alpha. Any math error here that would amplify the wrong star?

 d. **Proper motion drift.** `velocity * yearsSinceJ2000` — if
    `yearsSinceJ2000` runs large, any star that has huge proper
    motion could drift off-screen or close to camera in ways that
    change solidAngle. Mostly a non-issue at J2000 ±10 years but
    check the range.

 e. **Fragment halo texture sampling.** `profile = texture(u_starTex, uv).r`
    — the baked gaussian halo amplifies the perceived size beyond
    `gl_PointSize`. If the halo tex is different for stars of
    different color/bv, flag it. (It shouldn't be — single texture
    shared across all stars.)

### Part 3: Independent-source cross-check

Pull up Gaia Sky screenshots or documentation and sanity-check:
 - In Gaia Sky's default view (point sprite mode, no LightGlow or
   LensFlare active), at apparent-magnitude-dominated rendering, is
   Sirius visibly larger than Capella? Or are they actually similar?
 - Is our Sirius-at-50.5px calibration right? The `u_sizeFactor = 1.2e6`
   comes from `StarSetQuadComponent.java:96` defaults. Does Gaia Sky
   typically render its brightest star at ~50 px at 1080p?

### Part 4: Report format

Return:
  - **Summary** (1-2 sentences: does the math check out? is there a
    likely bug making Capella appear larger?)
  - **Math verification** (bullet list of checks, pass/fail)
  - **Pipeline drifts found** (priority-sorted if any)
  - **Unanswerable without running** (things that need a visual
    screenshot to settle)

## Files to inspect

Atlas (working directory):
 - `src/components/canvas/Starfield.tsx` (shader + material + geometry)
 - `src/lib/starPhysics.ts` (pseudo-size formula)
 - `src/lib/starfieldShaderMath.ts` (executable mirror)
 - `src/lib/starfieldShaderMath.test.ts` (regression tests)
 - `tasks/audit-top-stars-pixels.md` (generated table)
 - `scripts/audit-top-stars-pixels.mjs` (the script that built the table)

Gaia Sky (`/tmp/gaiasky`):
 - `core/src/gaiasky/util/coord/AstroUtils.java`
 - `core/src/gaiasky/util/Constants.java`
 - `assets/shader/star.group.quad.vertex.glsl`
 - `assets/shader/star.group.quad.fragment.glsl`
 - `core/src/gaiasky/scene/system/render/draw/StarSetQuadComponent.java`
 - `assets/conf/config.yaml`
